═══════════════════════════════════════════════════════════════════════════════
NOTES 9 — АУДИТ СКИЛЛОВ v2 (2026-06-10) — расширенная рубрика, пост-ремедиация
═══════════════════════════════════════════════════════════════════════════════

ПОВОД. Повторный прогон аудита (см. notes/8 = v1) ПОСЛЕ того, как v1-findings (38, 3 темы:
auth-optional / relational-optional / literals) были починены, + плюс новые правила:
  • новый запах scope_overclaim (под проблему dependencies.py: общий артефакт пере-сужен
    до одной фичи) — обнаружена пользователем вручную;
  • новый запах layer_leak (rule-10: скилл называет агентов/runner, прописывает порядок
    запуска других скиллов, тащит manifest-input-table) — ловились вручную грепом в v1.
Доп. цель: состязательная проверка СОБСТВЕННЫХ правок прошлой сессии (~18 скиллов) — не
внесены ли новые литералы/утечки.

МЕТОД. Workflow skill-altitude-audit-v2 (run wf_b975431a-d2c): 48 audit-агентов (6-запаховая
рубрика) → состязательная верификация по каждому скиллу → сборка. 67 агентов, ~3.0M токенов.

6 ЗАПАХОВ: app_specific_literal · optional_as_mandatory · wrong_altitude · cross_feature_coupling
· scope_overclaim (общий/многоцелевой артефакт определён как существующий ТОЛЬКО ради одной
фичи; инверсия optional_as_mandatory) · layer_leak (rule-10: имена агентов/runner / порядок
запуска / возврат-в-runner / manifest-input-table; ИСКЛЮЧЕНИЕ — conventions, санкционированный
derivation-registry).

РЕЗУЛЬТАТ. 48 проверено · 37 raised · 20 ПОДТВЕРЖДЕНО (17 отсеяно) · в 12 скиллах. 36 чистых.
  По запахам: app_specific_literal 8 · layer_leak 5 · scope_overclaim 2 · optional_as_mandatory 3
  · wrong_altitude 1 · cross_feature_coupling 1.

ГЛАВНЫЙ ВЫВОД. Re-audit отработал как самопроверка: 3 из 20 — баги, ВНЕСЁННЫЕ в прошлой сессии
(в т.ч. два моих новых скилла противоречат друг другу). Новые категории (layer_leak 5 +
scope_overclaim 2 = 7) v1-рубрика бы не нашла.

═══════════════════════════════════════════════════════════════════════════════
🔴 МОИ СОБСТВЕННЫЕ БАГИ ПРОШЛОЙ СЕССИИ (must-fix)
═══════════════════════════════════════════════════════════════════════════════

V2-1  test-store-repository-contract · app_specific_literal · HIGH  — РАССОГЛАСОВАНИЕ ДВУХ МОИХ СКИЛЛОВ
  Контракт-тест асертит `ProviderError`, но infra-store-repository (тоже мой, той же сессии)
  транслирует SDK-ошибки в `UpstreamError` (его Rule 10 «Mandatory fallback: UpstreamError»;
  все шаблоны `raise UpstreamError(...) from exc`). Тест проверяет исключение, которое адаптер
  НИКОГДА не бросает → `test_search_against_unreachable_store_raises_provider_error` упадёт
  против сгенерированного адаптера.
  LOC: line 77 `from ...exceptions import ProviderError`; line 134 `pytest.raises(ProviderError)`;
       description «ProviderError / NotFoundError»; Rule 6; Hard stop «asserts ProviderError / NotFoundError».
  FIX: свести к `UpstreamError` (network/store-fail) + `NotFoundError` (absent record); оформить как
       «доменное исключение, которое обещает boundary-трансляция» (manifest-declared placeholder),
       не литерал. Править description, line 77, 134, Rule 6, Hard stop. СВЕРИТЬ с infra-store-repository.

V2-2  restapi-app-bootstrap · cross_feature_coupling · HIGH  — КОММЕНТ ПРОТИВОРЕЧИТ КОДУ
  В Теме 2 добавлен коммент над lifespan «…never a hardcoded engine().dispose()», но сама строка
  `await container.engine().dispose()` ОСТАЛАСЬ в always-on main.py. Шаблон показывает ровно то,
  что запрещает его коммент. Не-relational приложение (qdrant/redis-only) копирует вызов
  провайдера, которого в контейнере нет.
  LOC: main.py `_lifespan` → `await container.engine().dispose()`.
  FIX: always-on шаблон делает teardown фиче-нейтральным — пустой (`yield` + коммент «контейнер
       диспоузит то, чем владеет, по графу датасторов»); `engine().dispose()` демотнуть в явно
       помеченный relational-вариант (как auth-файлы расщеплены на authed/public).

V2-3  test-integration-authed-client · scope_overclaim · MEDIUM  — ТОТ ЖЕ ТИП, ЧТО dependencies.py
  Я написал «auth-less app skips this skill, tests/integration/api/conftest.py, and the
  rsa_keypair/jwt_settings fixtures ENTIRELY». Но `api/conftest.py` — общий контейнер фикстур
  api-уровня (pytest-конвенция), не «auth-файл». auth (authed_client) — лишь его текущий
  единственный обитатель.
  LOC: opening «…skips this skill, tests/integration/api/conftest.py, and the … fixtures entirely».
  FIX: определить conftest по общему назначению (api-integration shared-fixture container);
       auth-less app просто не имеет authed_client/rsa_keypair/jwt_settings фикстур, а НЕ «файла
       целиком». Скипать auth-фикстуры, не файл.

V2-4  restapi-app-bootstrap · layer_leak · MEDIUM  — ОСТАТОК ПОСЛЕ ФИКСА «Catalog ordering»
  Тело «Catalog ordering» переформулировано в Теме 2/доп., но во ФРОНТМАТТЕРЕ description осталось
  императивное «Run this skill first, then per-resource skills can land their endpoints in
  app.include_router(...)» — секвенсинг = оркестрация.
  LOC: description, последняя фраза.
  FIX: структурный факт вместо команды: «per-resource skills land their work inside the shell this
       skill creates, so the shell is their structural precondition» — без «Run … first, then …».

═══════════════════════════════════════════════════════════════════════════════
🟠 НОВЫЕ КАТЕГОРИИ (v1-рубрика бы пропустила) — настоящие
═══════════════════════════════════════════════════════════════════════════════

V2-5  restapi-middleware · layer_leak · MEDIUM
  Rule 2 описывает СХЕМУ манифеста: называет путь `restapi.middlewares[*].config`, объявляет его
  «the one sanctioned open key→value map in the schema» + earn-its-place защита. Это
  manifest-schema/architecture-review знание, не артефакт-знание.
  FIX: оставить артефакт-правило (config приходит как kwargs, хранится на self, передаётся
       `app.add_middleware(Cls, **config)`, валидируется конструктором); выкинуть привязку к
       `restapi.middlewares[*].config` и earn-its-place обоснование (живёт в схеме/conventions).

V2-6  infra-capability-adapter · layer_leak · MEDIUM
  Hard stop: «Spec has no `exception_map` for a method that can fail → request the mapping before
  writing». Поля `exception_map` в манифесте НЕТ (Capability несёт implements/adapter/role/
  settings/requires_packages/notes/sources). Invented input-table + «request before writing» =
  оркестрация-директива.
  FIX: «Spec/behaviour не говорит, какие SDK-ошибки бросает fallible-метод → выведи SDK→domain
       маппинг из SDK-доков и notes, + обязательный fallback (UpstreamError/AuthError)». Убрать
       имя `exception_map` и «request the mapping before writing».

V2-7  test-restapi-endpoint · layer_leak · MEDIUM
  Rule 7 кондишенит на `cross_org_visibility = tenant-scoped` — поля-призрака (грепом только в
  этом скилле, нет в SCHEMAS/conventions).
  FIX: derivable-конвенция без поля: «When a resource is tenant-scoped (endpoint reads rows owned
       by another tenant), assert cross-tenant read → 404, not 403». Tenancy выводится из
       auth-claim/repo-фильтра, не из declared-поля.

V2-8  test-discovery-invariants · layer_leak · LOW
  When-to-use bullets: «runs first» / «runs before this» + Hard stop «install both first» — порядок
  запуска сиблинг-скиллов.
  FIX: оставить навигацию-указатели, убрать ordering-клаузы; выразить зависимость как факт о том,
       что тесты ИМПОРТИРУЮТ (нужны фикстуры real_app / authed_client), а не порядок.

V2-9  infra-di-provider · scope_overclaim · MEDIUM (+ optional_as_mandatory LOW, + stale-ref)
  • Line 136: «No providers.Resource … (the engine is disposed in the FastAPI lifespan)» — «the
    engine» как всегда-сущий (relational-only). FIX: «(a long-lived client/engine is disposed in
    the lifespan, when one exists)».
  • Line 16: «The lifespan teardown of a connection pool … → restapi-router-endpoint covers …» —
    (а) pool презюмируется present; (б) `restapi-router-endpoint` — НЕСУЩЕСТВУЮЩИЙ скилл
    (правильно restapi-endpoint / restapi-app-bootstrap). FIX: «teardown of any long-lived
    connection (engine/client pool, when the graph wires one) → handled in restapi/main.py» +
    починить имя скилла.

═══════════════════════════════════════════════════════════════════════════════
🟡 ДОРЕМЕДИАЦИОННЫЙ ХВОСТ (литералы / высота)
═══════════════════════════════════════════════════════════════════════════════

V2-10 test-discovery-invariants · app_specific_literal · MEDIUM
  `assert ...WWW-Authenticate...startswith('Bearer realm="myapp"')` — заморожен формат challenge
  (scheme+realm=app-root) исходного приложения. Скилл иначе строго дерайвит из real_app (CORS/size).
  FIX: асертить только load-bearing инвариант — что challenge ПРИСУТСТВУЕТ (non-empty, max
       `.startswith("Bearer")`), либо читать из auth-dependency. + Hard stop против frozen-формата.

V2-11 test-discovery-invariants · app_specific_literal · MEDIUM
  `assert body["code"] == "UNAUTHORIZED"` — литерал error-code. Противоречит дисциплине скилла
  (description «with the documented code»; Rule 4 + OpenAPI-тест дерайвят из route.responses).
  FIX: дерайвить ожидаемый code из 401-entry, что route advertises (error_responses/route.responses),
       не пинить «UNAUTHORIZED».

V2-12 test-restapi-endpoint · app_specific_literal · MEDIUM
  Streaming-download скелет: `content-type == "image/png"` + `attachment` заморожены (image-
  attachment фича исходного приложения).
  FIX: параметризовать media_type (матчить route.media_type per restapi-file-transfer) + нота, что
       inline/attachment и тип per-resource, не универсально image/png.

V2-13 infra-sqlalchemy-repository · wrong_altitude · MEDIUM
  `list` хардкодит `order_by(foos_table.c.created_at.desc())`, игнорируя `filter.sort`. Противоречит
  domain-filter Rule 6 («перевод sort-key→колонка живёт в repo-адаптере» — т.е. ЗДЕСЬ).
  FIX: учить правило: order_by дерайвится из filter.sort (мини-транслятор `_SORT_COLUMNS` / match по
       sort-enum → `.asc()/.desc()`), `created_at.desc()` — только placeholder-иллюстрация. Обновить Rule 9.

V2-14 restapi-file-transfer · optional_as_mandatory · MEDIUM
  «Safe only because MaxRequestSizeMiddleware caps the request» — middleware подан как always-present
  фундамент, делающий `await file.read()` безопасным. Middleware per-app (restapi.middlewares).
  FIX: размерный cap как precondition: «unbounded file.read() safe ТОЛЬКО за request-size cap; если
       app не объявил size-middleware (restapi-middleware), route не должен считать тело bounded».

V2-15 restapi-file-transfer · optional_as_mandatory · MEDIUM
  «Always advertise 413 … its code is registered in MIDDLEWARE_ERRORS» + `413` в шаблонах декораторов
  (lines 27, 73) — презюмирует size-middleware. Без него 413 неоткуда, и discovery-OpenAPI-тест
  может отвергнуть.
  FIX: гейтить 413 на наличие size-middleware («advertise 413 only when the app declares a
       request-size cap middleware»).

V2-16 restapi-file-transfer · app_specific_literal · LOW
  «Reuse this exact shape across routers — clients' file managers sort and dedupe based on it» +
  «default convention is ASCII timestamps» — формат имени (`%Y%m%d-%H%M%S`, `<project>-<resource>`)
  заморожен как канон с app-specific обоснованием.
  FIX: подать `_<purpose>_filename(ext)` как разумный дефолт-shape, выкинуть «reuse this EXACT shape
       / clients sort and dedupe» канон; формат — app-level выбор.

V2-17 general-imports-conventions · app_specific_literal · MEDIUM
  `from myapp.domain.auth import CurrentUser  # cross-package` — конкретный Helpdesk-инстанс
  (auth-сабдомен + auth-фича CurrentUser), хотя строка выше уже использует placeholder
  `from myapp.domain.foos import IFooRepository`.
  FIX: placeholder, напр. `from myapp.domain.bars import Bar  # cross-package — absolute`.

V2-18 general-imports-conventions · app_specific_literal · MEDIUM
  Layer-package re-export пример: `from . import auth, support` + `__all__ = auth.__all__ +
  support.__all__` — конкретные Helpdesk-сабдомены.
  FIX: placeholders — `from . import foos, bars` + `__all__ = foos.__all__ + bars.__all__`.

V2-19 test-integration-isolation · app_specific_literal · LOW
  `MinioContainer("minio/minio:latest")` — плавающий :latest, тогда как сиблинг postgres запинен
  `postgres:17-alpine`. Невоспроизводимо + неконсистентно.
  FIX: запиненный тег (RELEASE.<date> / digest) + строка прозы «testcontainer image tag — per-deployment
       pin, not frozen constant» (зеркалит conventions «no floating versions»).

(V2-9 line-16 stale-ref `restapi-router-endpoint` — учтён в V2-9.)

═══════════════════════════════════════════════════════════════════════════════
ПОРЯДОК ПОЧИНКИ (предложение)
═══════════════════════════════════════════════════════════════════════════════
1. 🔴 V2-1..V2-4 — реальные баги/рассогласования (V2-1 и V2-2 — функционально ломают сген. дерево).
2. 🟠 V2-5..V2-9 — layer_leak/scope_overclaim (новые категории, чистота слоёв).
3. 🟡 V2-10..V2-19 — литералы/высота.

ЧИСТЫХ 36. Идиомы для починки — те же: feature-conditional sub-Template / safe-default+comment /
derive-from-running-app / artifact-level (не называть агентов/схему). НЕ забыть: после правок
test-store-repository-contract СВЕРИТЬ исключения с infra-store-repository (V2-1).

ПРОВЕРКА ПОСЛЕ ПРАВОК: `uv run pytest .claude/tools/test_validate_manifest.py -q` (мета-тест
классификации скиллов; правки прозы валидатор не парсит, но мета-тест должен остаться зелёным 38).
Full workflow result: /private/tmp/.../tasks/w8z5spbtm.output (эфемерно). Скрипт воркфлоу:
.claude/.../workflows/scripts/skill-altitude-audit-v2-wf_b975431a-d2c.js (можно перезапустить для re-verify).
