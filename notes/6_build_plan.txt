План имплементации редизайна (агентный воркфлоу без генератора)

Ветка: workflow-without-generator. Источник правды дизайна: codegen_workflow_spec.md.
Каждый блок самодостаточен — режь и делегируй агенту: Задача · Контекст+ссылки · Что сделать ·
Готово когда · Зависит от. Строим прямо в .claude/ (упаковка в плагины — потом).

Уровни:
  A. ЯДРО forward-пути (§13) — 5 шагов ✅ ВСЕ ГОТОВЫ (forward-путь доказан на helpdesk + vector_rag e2e).
  B. UPSTREAM — analyst/architect агенты + слэш-команды (после forward-пути; до них манифесты пишем руками).
  C. ОТЛОЖЕННОЕ (§13 «потом») — brownfield, упаковка/мультиязык, снос архива.

═══════════════════════════════════════════════════════════════════════════════
A. ЯДРО forward-пути (§13)
═══════════════════════════════════════════════════════════════════════════════

─── ШАГ 1 — stdlib-валидатор ─── ✅ DONE (коммит 1d2cf32)
.claude/tools/validate_manifest.py (stdlib-логика + PyYAML через PEP 723) + 25 тестов +
fixtures/ (helpdesk/vector_rag/label). Форма + целостность графа + rule #11 + громкая
деградация + sources. Ходит по объявленным рёбрам, типы/сигнатуры НЕ парсит.

─── ШАГ 2 — Слой знаний (скиллы + conventions) ─── ✅ DONE (коммиты 9b91e17, 986533b)
Conventions-reference-скилл .claude/skills/conventions/SKILL.md — единственный дом деривации:
kind→путь/класс/суффикс (порт naming.py + таблица ПЕРЕНЕСЕНА из MANIFEST_SCHEMA.md, там указатель),
реестр kind→skill (таксономия producer/companion/test/bootstrap/reference/meta), профили сторов
(порт store_profiles.py), субстрат СПИСКОМ без версий (константы engine.py), команды тулчейна.
Гейт §16 вписан в validate_manifest.py (KIND_TO_SKILL + presence-gap + мета-тест покрытия).
2a: 0 генератор-протечек в формулировках, НО сверка деривации вскрыла дрейф ~11 файлов → приведены
к канонам (выбор пользователя): инфра по ТЕХНОЛОГИИ (infrastructure/postgres/, capability по adapter —
не db/ и не by-concern storage|auth|rendering) + application по САБДОМЕНУ (выводится из агрегата
хендлера); infra-sqlalchemy-table переформулирован (таблица = write-once скаффолд, миграции НЕ
генерятся — цепочкой владеет Alembic). Follow-up'ы решены: domain.filters сделан первоклассной секцией
валидатора (Filter/FilterSort); нереляционный repo-скилл ОТЛОЖЕН (JIT/§16 — при первом qdrant/redis
репозитории, тогда же диспатч infrastructure.repositories станет store-kind-aware).
ИТОГ: 31 тест зелёный, 3 фикстуры (helpdesk/label/vector_rag) валидируются чисто.

─── ШАГ 3 — Агент-скаффолдер ─── ✅ DONE (b127387 .claude/agents/scaffolder.md; d03e98c /scaffold)
ЗАДАЧА. Агент читает валидный манифест + скиллы + conventions и раскладывает ВСЕ файлы: декларатив,
  glue (импорты/DI/__init__/роуты/депенденси-манифест), каркасы тел (class+сигнатура+контракт-
  коммент+NotImplementedError), красные каноничные тесты. Файлы создаёт ТОЛЬКО он.
КОНТЕКСТ. spec §3 (что создаёт + слой знаний), §4 (владение: декларатив/glue регенерится всегда,
  тело write-once), §9 (красные тесты из behaviour; flat vs manual-stub шов), §11 (single-pass, не
  параллелится). Размещение верифицирует ТУЛЧЕЙН: каркасы компилируются с NotImplementedError →
  неверный путь = битый импорт = красный. Роль — .claude/agents/scaffolder.md.
ЧТО СДЕЛАТЬ. Написать промпт+процесс: обход графа в порядке зависимостей → на узел вывести путь/имя/
  скилл из conventions → эмитить файл(ы). Декларатив/glue — целиком из Template скилла. Тело —
  каркас + контракт-коммент (из behaviour/notes) + красный тест (flat-словарь → генерим тест целиком;
  реляция/время/негатив → manual-stub, §9). Зафиксировать детерминированный контракт вызова (какой
  срез манифеста + какой скилл инжектится на узел — §2: различие через контекст, НЕ форк промпта).
  (Тут же опционально сработал бы skill-gap гейт §16 — но он требует реестра kind→skill; на Helpdesk
  все kind'ы покрыты.)
ГОТОВО КОГДА. Прогон на Helpdesk: импорты резолвятся, mypy зелёный на каркасах-с-NotImplementedError,
  красные тесты падают на NotImplementedError. Тел ещё нет.
ЗАВИСИТ ОТ. Шаг 2.

─── ШАГ 4 — Агент-имплементер + петля верификации ─── ✅ DONE (439ddd3 implementer+verify runner; d1e5568 hardening)
ЗАДАЧА. Агент заполняет каркасные тела за контрактом до зелёного тулчейна/тестов; + детерминированный
  триггер раннера (нашёл NotImplementedError/красноту на тело-несущем узле → будит имплементера) +
  петля верификации.
КОНТЕКСТ. spec §4 (жизненный цикл; дрейф контракта → красный → имплементер догоняет), §9 (каноничные
  тесты = единица доверия; АНТИ-СГОВОР: тело пишется отдельно/после теста, manual-assert НЕ читать),
  §11 (параллель по DAG-уровням ТОЛЬКО на телах), §12 (TDD-режим), §0 принцип 3 (детерминизм в
  верификаторе). Существующий .claude/agents/implementer.md — ПЕРЕПИСАТЬ под этот мир.
ЧТО СДЕЛАТЬ. Переписать implementer.md: дан один каркасный узел + его скилл (kind→skill) + контракт →
  написать тело; гонять каноничный тест узла + mypy до зелёного; файлы НЕ создавать; manual-assert
  НЕ читать. Описать детерминированный триггер раннера + топо-раскладку (параллель внутри уровня,
  уровни последовательно). Адверсарный верификатор (§9) — второй агент пытается сломать ассерт на
  рукописном хвосте. Решить форму петли: /verify-команда + раннер-скрипт ИЛИ агент-driven (сейчас —
  /verify + ручной диспатч, пока авто-раннер не построен).
ГОТОВО КОГДА. На Helpdesk все тела заполнены → mypy + ruff + все каноничные/manual тесты зелёные.
ЗАВИСИТ ОТ. Шаг 3.

─── ШАГ 5 — Первый эпик e2e (Helpdesk) без генератора ─── ✅ DONE (helpdesk5 в 6 DAG-волн; vector_rag e2e; d484ddc/d1122e5/a3b6b4a hardening)
ОТКРЫТЫЙ ШОВ. Docker-less integration (testcontainers Postgres/MinIO) не прогонялся — гонялись construct + unit.
ЗАДАЧА. Прогнать весь forward-путь validate→scaffold→implement→verify на Helpdesk → рабочий бэкенд,
  зелёные тесты, генератор не участвует. Приёмочный гейт редизайна.
КОНТЕКСТ. spec §13 шаг 5, §14 (открытые вопросы всплывут тут: надёжность размещения скаффолдером,
  достаточность контракт-коммента, форма каноничного теста на толстом узле). Манифест:
  .claude/tools/fixtures/helpdesk_manifest.yaml. Вывод — в disposable git-ignored папку.
ЧТО СДЕЛАТЬ. Связать последовательность (вручную или тонким раннером/командами):
  validate_manifest.py → scaffolder → implementer (по DAG) → петля верификации. Гонять до: mypy/ruff
  чисто, все тесты зелёные, приложение импортируется/стартует. Собрать findings против §14 (где не
  хватило интента, где скаффолдер промахнулся путём, где понадобился manual-stub) → обратно в
  скиллы/conventions/манифест.
ГОТОВО КОГДА. Зелёный Helpdesk-бэкенд, целиком собранный агентным путём + короткое ретро по §14.
ЗАВИСИТ ОТ. Шаги 2-4.

═══════════════════════════════════════════════════════════════════════════════
B. UPSTREAM (полный пайплайн, после forward-пути)
═══════════════════════════════════════════════════════════════════════════════
До него манифесты можно писать руками (Helpdesk есть). Детальные брифы — когда дойдём.
Ссылки: spec §1 (стадии), §2 (исполнители).

  B1. Analyst-агент + /ingest-usecases + /refine-usecases.
      UC → эпики (по Module + связанности агрегатов), фильтр бэкенда (UI-формулировка иногда несёт
      бэкенд-инвариант → не скипать молча), продуктовые вопросы к BA ФАЙЛОВЫМ каналом
      (questions_for_ba.md, пачкой). Выход: UC без TBD.
  B2. Architect-агент + /build-manifest + /apply-delta.
      UC → манифест/дельта (один bounded context, §7), архитектурные вопросы к ТЕБЕ в чат, проставляет
      sources, переводит UC в дельту (supersedes/replaces, §8). Ты ревьюишь дельту ~5 строк.
  B3. Слэш-команды: /validate-manifest (обёртка над .claude/tools/validate_manifest.py), /scaffold
      (зовёт scaffolder), /verify (петля).

═══════════════════════════════════════════════════════════════════════════════
C. ОТЛОЖЕННОЕ (§13 «потом»)
═══════════════════════════════════════════════════════════════════════════════
  C1. Brownfield-фронтир: GC орфанов (узел убран из манифеста → старые файлы — мёртвый груз) +
      rename-с-переносом-тела (rename агрегата теряет заполненное тело). «Кого» считает граф; «как» —
      ревьюимый replaces/removal в дельте, не догадка. spec §4/§8.
  C2. Упаковка + мультиязычность: marketplace {codegen-core + пак-плагины}; conventions → reference-
      скилл пака; размещение ролей scaffolder/implementer (ядро vs пак — решить, когда роли написаны).
      spec §15, §14.
  C3. Снос src/codegen (архив): только ПОСЛЕ приёмки агентного пути на эпике. И НЕ в main — main
      остаётся архивом генератора (фолбэк, если вернёшься к кодогенератору); ветку в main не мёрджим.

═══════════════════════════════════════════════════════════════════════════════
ИТОГО
═══════════════════════════════════════════════════════════════════════════════
Forward-путь: ✅ ГОТОВ (все 5 шагов, доказан на helpdesk + vector_rag e2e).
Осталось: B (upstream — analyst/architect + слэш-команды) + C (отложенное — brownfield, упаковка, снос архива).
