# UC-12 (refined) — Upload a meeting recording

> Refined from `specs/use-cases/UC-12-upload-recording.md` (verbatim, unchanged). Folds in the BA's
> answers from `questions_for_ba.md` and resolves the product TBDs. Architecture questions stay open in
> `epic.md`.

**Actor**: Member, Admin (any signed-in user)
**Module**: Meetings / Upload

## Description

The core of MeetingMind: a user uploads an audio recording of a meeting, and the system stashes the
file and creates a meeting record that the AI pipeline (UC-13) will later enrich. This UC is just the
**ingest** half — get the bytes safely stored and a row created; transcription/summary happen in UC-13.

Storage is split deliberately: the raw audio is large and binary, so it goes to blob storage (S3 or
compatible), and only a reference (the blob key) plus metadata live in the relational database. The
team was firm that we must not end up with a database row pointing at a blob that failed to upload, or
a blob with no row — so the upload-then-record sequence has to undo the blob if the row write fails.

For v1, plans cap usage by **number of meetings created in the calendar month** against the plan's
limit, checked at upload time, **before** the upload is stored. The count resets at the start of each
calendar month, using UTC month boundaries for v1 (per-workspace timezone is out of scope). Product's
eventual target is "minutes of audio per month", but that needs a recording's duration, which isn't
known until transcription (UC-13); minutes-of-audio is therefore deferred until duration is available
earlier in the pipeline. (Product accepts that for v1 a 3-hour meeting and a 2-minute one count the
same.)

## Main flow

1. User picks an audio file and uploads it (multipart form: the file plus a title).
   - **File** (required, audio; an `UploadFile`).
   - **Title** (required, 3–200 chars after trimming).
2. System checks the workspace is within its monthly quota for its plan (see Business Rules + UC-11).
3. System uploads the audio bytes to blob storage and gets back a storage key.
4. System creates a meeting record: a fresh id, the title, the blob key, status **UPLOADED**, the
   uploader as `created_by`, and the workspace from the token. Duration/transcript/summary are empty
   until UC-13.
5. System returns the new meeting id (the client then triggers processing, UC-13, as a separate
   explicit call).

## Alternative flows

- **A1**: The workspace is over its monthly quota. System refuses with a "quota exceeded" error
  (the quota is checked before any blob upload). No blob is uploaded, no row. (The exact HTTP status —
  402 Payment Required vs 403 — is an API-contract choice left for the architect; see `epic.md`.)
- **A2**: The blob upload to storage fails. System surfaces an "upload failed" error; nothing is
  recorded (there's nothing to undo yet).
- **A3**: The blob uploaded but the database write then fails. System **deletes the just-uploaded
  blob** (so we don't leak an orphan) and surfaces the error. This compensating step is mandatory.
- **A4**: File too large. The maximum upload size is **500 MB**, enforced by the platform's
  request-size middleware (the limit is deployment config, not hard-coded); an over-limit upload is
  rejected with HTTP 413. For v1 the system does **not** sniff or validate the audio format — there is
  no allowed-formats list and no magic-byte check. A non-audio or unsupported file is accepted at
  upload and simply fails downstream during processing (UC-13 → FAILED). An explicit allowed-formats
  list (e.g. wav/mp3/m4a) is a fast-follow, not v1 scope.

## Business Rules

- The uploader's identity (`created_by`) and the `workspace_id` come from the authenticated token,
  never from the request body (the reporter-stamp rule, applies everywhere).
- A meeting always belongs to exactly one workspace; every later query is scoped by it.
- Quota is enforced at upload, by meeting count per calendar month per workspace, against the plan's
  limit; the count resets at each UTC calendar-month boundary. The per-tier numbers are deployment
  config, not hard-coded.
- No database row may reference a blob that isn't stored, and no stored blob may lack a row (A3).

## Notes

- The plan→limit numbers must be tunable from config (security/billing retune them without a deploy).
- Synchronous vs async: the upload returns as soon as the row is created; the heavy AI work is a
  separate, explicit call (UC-13). Upload (UC-12) and processing (UC-13) are two separate calls in v1 —
  processing is **not** kicked off automatically. Auto-kickoff and async/background processing are
  deferred to a future UC.
- Out of scope: resumable/chunked uploads, virus scanning. Flag for later.

## Refinement log

- Q1 (quota unit): v1 caps by **number of meetings created in the calendar month** against the
  workspace plan, reset at each UTC calendar-month boundary, checked **before** the upload is stored;
  minutes-of-audio deferred → applied to the Description, Main flow step 2, A1, and the quota Business
  Rule.
- Q2 (max file size / formats): **500 MB** max, enforced by request-size middleware (config; 413 on
  over-limit); **no** format sniffing or allowed-formats list in v1, bad files fail downstream in UC-13
  → applied to A4.
- Q3 (explicit vs automatic processing): upload and processing are two separate, explicit v1 calls; no
  auto-kickoff, async deferred → applied to Main flow step 5 and the synchronous-vs-async Note.
