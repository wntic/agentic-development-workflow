# UC-10 (refined) — Sign in to the workspace

> Refined from `specs/use-cases/UC-10-sign-in.md` (verbatim, unchanged). Folds in the BA's answers
> from `questions_for_ba.md` and resolves the product TBDs. Architecture questions stay open in `epic.md`.

**Actor**: Member, Admin (any registered user)
**Module**: Accounts / Authentication

## Description

MeetingMind is a multi-tenant SaaS: every customer is a **workspace**, and every person belongs to a
workspace. There is no public sign-up in v1 — a workspace and its first admin are provisioned by us
during onboarding, and the admin invites members (invitations are a later UC, not this one). Signing
in is the front door: a user submits email + password and, if they match, gets a signed bearer token
that every other use case assumes.

We debated whether a person can belong to more than one workspace (consultants who serve several
clients asked for it). We decided **one workspace per user for v1** — multi-workspace membership is a
real future need but it complicates the token and every query's tenant scoping, so it's deferred. The
token therefore carries the user's id, their role, and their single workspace id; every tenant-scoped
query derives the tenant from that single `workspace_id` claim, never from a request parameter. The
multi-workspace switching machinery is firmly deferred. We considered SSO / Google login but it is
**out of v1** — v1 is email + password only (the single sign-in path); SSO / Google is deferred and
likely the first fast-follow after launch.

This is the same credential model the team used on a previous product, so argon2id + a JWT bearer is
the assumed baseline unless security objects.

## Main flow

1. User opens the sign-in screen and submits credentials. The form has fields:
   - **Email** (required, email; matched case-insensitively against the stored account).
   - **Password** (required, free text, min 8 chars to even attempt).
2. System validates the credentials:
   - Looks up the account by a lower-cased email.
   - Verifies the password against the stored hash (argon2id).
   - Confirms the account is active.
3. On success, System issues a signed bearer token (a JWT carrying the user id, role, **and the
   workspace id**) and returns it. The token's lifetime is **1 hour**, configured as deployment config
   (tunable per environment, not hard-coded). There is no refresh-token flow in v1: when the token
   expires the user signs in again. A successful sign-in also resets the failure counter (see A3)
   immediately.
4. Client stores the token and lands the user on their meeting library (see UC-14).

## Alternative flows

- **A1**: Wrong password or unknown email. System returns a generic "invalid email or password" — we
  do not reveal whether the email exists, and an unknown email still runs a dummy hash so timing
  doesn't leak existence.
- **A2**: Account is deactivated (`is_active=false`). Credentials may be correct but System refuses
  with an "account inactive" error; the user must contact their workspace admin.
- **A3**: Too many failed passwords. Failed-login lockout **ships in v1** as the baseline brute-force
  protection. After **5 failed attempts within a 10-minute rolling window** the account is locked for
  a **15-minute** cool-off period, after which the counter resets and sign-in is allowed again. All
  three numbers (threshold, window, lock duration) are **deployment config, not hard-coded** — so they
  can be tuned per environment without a release. A successful sign-in resets the failure counter
  immediately. The lock must not leak account existence: a locked account returns the same generic
  "invalid email or password" as any other failure (A1), never a distinct "account locked" signal.

## Business Rules

- No self-service registration in v1 — accounts exist because we (or a workspace admin via invite,
  later UC) created them.
- A user belongs to **exactly one workspace** in v1 (see Description — multi-workspace is deferred).
  The token carries exactly one `workspace_id`, and every tenant-scoped query derives the tenant from
  that single claim, never from a request parameter.
- We never reveal whether an email is registered — same generic error and same response timing for
  unknown-email and wrong-password (applies to all auth responses, see also UC-12/UC-14 which all sit
  behind this token). A locked account (A3) is covered by this same generic error.
- The bearer token is signed with the server-side key; clients send `Authorization: Bearer …`.

## Notes

- Password reset is **not** in v1: no "forgot password" link, no reset email, no reset token. The only
  recovery path is a **workspace admin re-setting the password** for the user — an admin capability
  tracked as its own future UC, not part of this sign-in UC. Self-service reset is a flagged
  fast-follow.
- The token must carry `workspace_id` because every meeting query is tenant-scoped by it — if we ever
  do multi-workspace, this is the thing that breaks first, so keep it isolated.
- Token TTL for v1 is **1 hour** (matching the previous product), set as deployment config so it can be
  tuned per environment. The longer-lived batch/ML tokens stay out of scope for v1.

## Refinement log

- Q1 (Account lockout — in v1 scope): lockout **ships in v1** as baseline brute-force protection, and a
  locked account returns the same generic "invalid email or password" (no existence leak) → applied to
  A3 and the existence-leak Business Rule.
- Q2 (Lockout thresholds and cool-off): **5 failed attempts within a 10-minute rolling window** trips a
  **15-minute** lock; counter resets after the lock and on any successful sign-in; all three numbers are
  **deployment config, not hard-coded** (security signed off on the starting proposal as the default) →
  resolved the TBD in A3, with the counter-reset-on-success note added to Main flow step 3.
- Q3 (Bearer token TTL): **1 hour**, as **deployment config**; no refresh-token flow in v1; longer-lived
  batch/ML tokens out of scope → resolved the TBD in Notes and added the TTL/no-refresh detail to Main
  flow step 3.
- Q4 (Password reset — out of v1): confirmed **no self-service reset** (no link, email, or token); only
  recovery is a workspace admin re-setting the password, tracked as a future admin UC → applied to the
  Notes password-reset bullet.
- Q5 (SSO / Google login — deferred): confirmed **v1 is email + password only** (single sign-in path);
  SSO / Google deferred as a likely first fast-follow → applied to the Description.
- Q6 (One workspace per user — v1): confirmed **one workspace per user**; token carries exactly one
  `workspace_id`, every tenant-scoped query derives the tenant from that single claim (never a request
  parameter); multi-workspace firmly deferred → applied to the Description and the one-workspace
  Business Rule.
