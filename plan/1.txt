F-01: можешь использовать формат `adw:<skill-name>`, потому что все агенты будут жить внутри этого воркфлоу, который пакуется в плагин. То есть ситуации, когда плагин не будет установлен не будет.

F-05: дописываем .PHONY: check в WORKFLOW.md

F-06: полагаю, что нужно отрефакторить testing-unit/SKILL.md и убрать из него артефакты предыдущего воркфлоу. 

F-07: я думаю, что test-author должен добавить этот маркер в pyproject.toml сеттинг раз уже он пишет тесты и использует эти маркеры. Однако здесь я увидел еще один недочет: мы используем маркеры AC и помечаем ими тесты но после /adw:accept нигде в спеках нельзя найти acceptance criteria, которые были и к которым относятся помеченные тесты. Это ожидаемо или нет?

F-08: не понимаю сути проблемы и не могу предложить какое-то решение.

F-10: аналогично не понимаю в чем суть проблемы и не могу предложить решения. Я предполагаю, что у меня в целом проблема со скиллами и с ними нужно конкретно что-то будет решать потом.

F-12: аналогично не понял сути проблемы.

F-13: реши какие маркеры тебе нужны и унифицируй их множество.

F-17: какое изменение нужно внести в канон?

F-23: в целом твои предложенные решения хороши. выбери наиболее подходящее. 

F-24: мы пока что тестируем плагин только локально. после того как все будет закончено ветка rebuild будет влита в main и все файлы-инструкции будут откорректированы.

F-25: проблема с кэшированием серьезная. как мы можем это исправить?

F-26: добавим версию в будущем, но пока что без нее.

F-27: не понял сути проблемы. у меня же есть отдельный репо для пробы @~/Projects/adw-probe/ и туда ставится плагин для тестов локально.

F-28: зачем нам вообще его просить писать NO-SKILL и давать эту проверку? предлагаю убрать это

F-18: как это исправить?

F-29/F-31: вот у меня здесь тоже возник вопрос. TDD предполагает, что сначала пишутся красные тесты, а потом реализация до тех пор пока тесты не позеленеют. Однако на первом проходе у нас даже нет нужных модулей, названий классов, методов/функций и тд. В пробе сервиса укорачивания ссылок пришлось обходиться костылями в виде if TYPE_CHECKING. Это причем касается не только первой спеки в пустом проекте. Я могу писать новую фичу-интеграцию где нужно будет написать новый модуль и тесты точно также будут падать по ModuleNotFoundError. И здесь еще такой момент, что мы как будто бы доверяем test-author агенту то как у нас будут организованы пакеты модулей, все названия. Тут нужно подумать глубже над решением.

F-32: можно просто вычистить всю память.

F-33: выбери подходящее решение.

F-34: пока оставим без субстрата для запуска приложения. пока что я буду отдельно добавлять docker-compose, .env файлы и тд. Чуть позже решим, что с этим делать.
