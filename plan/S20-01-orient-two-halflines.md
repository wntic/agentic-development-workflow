# S20-01 — две полустроки в `ORIENT.md` §4: номер с цитатой, чек-лист против каталога

Тип агента: **`adw-builder`**
Слой: **дев-запись**
Происхождение: решение человека 2026-08-10, `plan/findings/legacy.md`, секция «Решения человека,
2026-08-10 — шестая чистка (волна S20), дефекты файлов задач и дыры протокола», строки **F-200** и
**F-208**.

**Что это за задача.** Два урока прохода S10 получают дом в §4. Первый (F-200): номер строки `:328`
в «Границах» задачи был скопирован из задачи прошлого прохода, а не грепнут заново, и огородил чужое
живое правило; номера строк устаревают от каждой правки волны выше по файлу. Второй (F-208): пункт
чек-листа «Что скажет warden» требовал внести в шипящийся скилл словарь цикла — буквальное исполнение
нарушило бы стоящее правило «a skill must not know what invokes it», и warden, читающий буквально,
отклонил бы верную работу.

Читать сначала:

- `plan/ORIENT.md` §4 — таблица проверок (адресат первой полустроки — строка про **перечень
  владельцев**, «грепни дерево на артефакт, а не вспоминай») и абзац сразу после таблицы («И
  последним действием правки файла задачи **перечитывается блок „Проверка“ и чек-лист „Что скажет
  warden“…»», сейчас на строке ~134) — адресат второй;
- тела записей грепом по заголовкам, **не читая `legacy.md` целиком** (`ORIENT.md` §5):
  `rtk proxy grep -n '^## F-200 \|^## F-208 ' plan/findings/legacy.md`, затем ~28 строк каждое;
- строки решений по F-200 и F-208 в секции решений 2026-08-10 в конце того же файла.

## Задача

1. В строку таблицы про перечень владельцев (или её колонку «Сделай») — полстроки: **номер строки в
   файле задачи всегда идёт вместе с цитатой, и предмет опознаётся по цитате**; номер — удобство,
   цитата — предмет (F-200: номер, скопированный из задачи прошлого прохода, огородил чужое правило;
   номера устаревают от каждой правки волны выше по файлу).
2. В абзац перечитывания после таблицы — полстроки: чек-лист «Что скажет warden» перечитывается **и
   против стоящих правил каталога**, не только против Deliverables (F-208: буквальное исполнение
   пункта требовало нарушить правило скилла, и warden отклонил бы верную работу).
3. Ничего больше: другие строки таблицы, абзацы §4 и правило F-151/F-154 в абзаце перечитывания не
   переформулируются — вторая полустрока дописывается к нему, а не заменяет его.

## Deliverables

Правка в одном файле `plan/ORIENT.md`. Больше ничего.

## Границы

- `CLAUDE.md`, `plan/INDEX.md`, `plan/findings/**`, `plugins/**`, `.claude/**` — не трогать.
- Никакого механизма: обе полустроки — совет в карте, не проверка.
- Не чинить найденное по дороге: находка — в отчёт.

## Проверка

Прогнана на нетронутом дереве до отправки задачи; ответ «до» назван у каждой строки.

```bash
rtk proxy grep -c 'F-200\|F-208' plan/ORIENT.md
# до: 0. После: 2 — оба урока несут свои номера
rtk proxy grep -c 'номер строки' plan/ORIENT.md
# до: 0. После: ≥1 — первая полустрока стоит
rtk proxy grep -c 'правилами каталога\|правил каталога' plan/ORIENT.md
# до: 0. После: ≥1 — вторая полустрока стоит
rtk proxy grep -c 'перечитывается блок' plan/ORIENT.md
# до: 1. После: 1 — абзац перечитывания дописан, а не заведён заново
git diff --stat -- plan/ORIENT.md
# до: пусто. После: один файл — вклад задачи только здесь
rtk proxy find plugins/adw -name '*.py' -o -name '*.sh' -o -name 'hooks.json' -o -name 'anchors.json'
# до: пусто. После: пусто — красная линия 2
```

## Что скажет warden

- дифф — один файл `plan/ORIENT.md`, две вставки: строка таблицы и абзац перечитывания; ничего не
  переписано заново;
- обе полустроки цитируют свои номера (F-200, F-208) как данные, без пересказа тел;
- правило F-151/F-154 в абзаце перечитывания сохранено дословно или эквивалентно;
- механизма нет.
