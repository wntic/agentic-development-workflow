# S09-05 — вторая проба приводится к четырём правкам каталога

Тип агента: **`adw-builder`**
Слой: **вне репозитория** (`~/Projects/adw-rooms`, ветка `main`)
Зависит от: **S09-01, S09-02, S09-03, S09-04** — все четыре должны быть закоммичены, потому что проба
приводится к правилам, которые они пишут. Это та же зависимость, что у S07-02 от S07-01 и у S08-06
от S08-03.
Происхождение: решения человека 2026-08-05, `plan/FINDINGS.md`, секция «Решения человека, 2026-08-05 —
три замечания по коду второй пробы», строки **F-167, F-168, F-169, F-170**.

Читать сначала:
- в `plan/FINDINGS.md` — тела **F-167, F-168, F-169, F-170** заголовочным грепом;
- четыре правленых скилла в их **новом** состоянии: `testing-integration-setup`, `conventions`,
  `restapi-app`, `test-principles`. Проба приводится к тому, что там написано **сейчас**, а не к
  пересказу в этом файле;
- в самой пробе: `pyproject.toml`, `tests/integration/conftest.py`,
  `src/rooms/restapi/main.py`, `tests/integration/api/bookings/test_booking_survives_a_second_process.py`.

Change 001 **принят человеком**. Это housekeeping отдельным коммитом в `main` пробы, а не правка
изменения: цикл `/adw:spec` → `/adw:build` → `/adw:accept` здесь не запускается.

## Задача

Четыре правки, все замерены на дереве пробы главной сессией — адреса ниже проверены, но **сверь их
сам**, файл мог сдвинуться.

**1. F-167 — импорт testcontainers.** `tests/integration/conftest.py:42`:
`from testcontainers.postgres import PostgresContainer` →
`from testcontainers.community.postgres import PostgresContainer`. Единственное вхождение: minio и
qdrant в этой пробе не заведены.

**2. F-167, хвост — флор.** `pyproject.toml`, `[dependency-groups] dev`: `"testcontainers"` →
`"testcontainers>=4.15"` с причиной, в форме, которую написала S09-02. Установленная версия 4.15.0,
так что `uv lock` не сдвинется — но проверь, что не сдвинулся, и скажи об этом в отчёте.

**3. F-169 — две аннотации под `@asynccontextmanager`, и ровно две.**
`src/rooms/restapi/main.py:1` и `:17` — `AsyncIterator` → `AsyncGenerator`, форма `AsyncGenerator[None]`.
`tests/integration/api/bookings/test_booking_survives_a_second_process.py:21` и `:79` — то же для
`_running_process`, форма `AsyncGenerator[str]`.
Импорты в обоих файлах привести: `AsyncIterator` там больше не нужен — **если он в файле больше нигде
не используется**; проверь, а не предполагай.

**4. F-170 — `filterwarnings`.** `pyproject.toml`, `[tool.pytest.ini_options]`:
`filterwarnings = ["error"]`, в форме, которую написала S09-04.

**Это самая рискованная правка задачи, и её результат — часть отчёта.** После неё `make check` может
покраснеть на предупреждениях, о которых никто не знал. Разбирай их по водоразделу, который написала
S09-04:

- предупреждение из **нашего кода** (`src/`, `tests/`, `migrations/`) — **чинится**, не глушится;
- предупреждение **чужой библиотеки**, которое проект починить не может, — получает строку
  `"ignore:…"` **с причиной рядом**: чьё, почему неустранимо, что снимет исключение;
- если чужих предупреждений окажется больше двух-трёх, **остановись и доложи списком**, не глуши
  пачкой. Список исключений длиннее правила — это выключенное правило, и решение о нём человека.

**Миграцию не трогать.** `migrations/versions/0001_initial.py` уже написан правильной формой — это
он и есть образец, по которому S09-02 переписала скилл. Правка F-168 в пробе **уже исполнена**
имплементером 001.

## Deliverables

Четыре файла пробы, один коммит в `main` пробы. Сообщение коммита — по-английски, как весь код пробы;
что́ оно объясняет — **почему**, а не перечень файлов.

В этом репозитории (`agentic-development-workflow`) — **ноль изменений**. Ты не пишешь сюда ничего:
ни в `plan/`, ни в `FINDINGS.md`.

## Границы

- **Только четыре названных файла пробы.** Ни `Makefile`, ни `migrations/`, ни живая спека
  `specs/`, ни `README.md`.
- **Никакого рефакторинга по дороге.** Нашёл рядом проблему — в отчёт словами, не в дифф. Находки в
  `FINDINGS.md` пишет главная сессия.
- **`AsyncIterator` у фикстур pytest не трогать** — таких аннотаций в пробе четыре
  (`conftest.py:112`, `:124`, `:158` и тест-файл `:110`), и они верны: устаревание
  висит на `@asynccontextmanager`, а под фикстурой его нет. Замерено: `@asynccontextmanager` в пробе
  ровно два — `main.py:16` и тест-файл `:78`.
- **`[tool.mypy] module = [… "testcontainers.*"]` не трогать** — глоб покрывает
  `testcontainers.community.*`.
- **Не запускать `/adw:*`.** Это housekeeping, а не изменение.
- **`uv.lock` коммить, если он сдвинулся**, и сказать об этом; не сдвинулся — не трогать.

## Проверка

```bash
cd ~/Projects/adw-rooms
rtk proxy grep -rn 'from testcontainers\.' tests/        # одна строка, через .community.
rtk proxy grep -rn 'asynccontextmanager' src/ tests/     # четыре строки: импорт+декоратор в двух файлах
rtk proxy grep -rn 'AsyncIterator' src/                  # пусто
rtk proxy grep -rn 'AsyncGenerator' src/ tests/          # четыре строки: импорт+сигнатура в двух файлах
rtk proxy grep -n 'testcontainers\|filterwarnings' pyproject.toml
make check                                               # зелёный целиком
git diff --stat -- . ':!uv.lock'                         # четыре файла
```

`make check` — не формальность и не последний шаг: он поднимает настоящий PostgreSQL в контейнере и
он единственное доказательство, что `filterwarnings = ["error"]` не покрасил суиту. Прогони его
**после** всех четырёх правок и приведи хвост вывода в отчёте — число тестов и число предупреждений.

## Что скажет warden

- **основание измерено** — четыре находки с командами, все в реестре до этого файла;
- **правки каталога закоммичены раньше** — проба приводится к дереву, а не к пересказу;
- **скриптов в пробе не прибавилось**, `make check` остался тем же из четырёх строк;
- **исключения `filterwarnings`, если появились, несут причину** и их не больше, чем правило допускает;
- **фикстуры не тронуты** — правка узкая по замеру, а не по осторожности.
