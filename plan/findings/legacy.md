Открытые записи замороженной серии F, перенесены из `plan/FINDINGS.md` 2026-08-09 (S13-01).
Формат и правила — `plan/ORIENT.md` §5; серия заморожена на F-284, новые находки живут файлами на источник.

## F-197 · Ссылок на правило по ординалу в каталоге не одна, а восемь, и четыре из них межфайловые — довод диспозиции F-174 на них не работает
Найдено: builder S10-06 (назвал класс), главная сессия перемерила и **уточнила число**
Класс: ИЗМЕРЕНО.

```bash
rtk proxy grep -rn 'rule [0-9]\|rules [0-9]' plugins/adw/skills/ | rtk proxy grep -v 'test-principles'
```

**Межфайловые — четыре:** `test-application-handler:171` («mirrors `application` DTO rule 2»),
`test-fake-repository:199` («see `test-application-handler` rule 10»), `restapi-schema:99`
(«`domain-model`, filter-record rule 5»), `infra-store-repository:226` («`infra-capability-adapter`
rules 13–15»). **Внутрифайловые — в двух файлах:** `infra-wiring:286` («(rule 12)») и
`meta-skill-author` (`:88`, `:229`, `:232`, `:247` — все про собственные правила 1 и 9).

Builder доложил «шесть, из них пять межфайловых». Перемер даёт **четыре** межфайловых, и это тот же класс,
что F-158 (три стороны посчитали одно число и разошлись), только здесь разошлись две. Число в записи —
моё, добытое приведённой командой; довод класса — исполнителя, и он верен.

Что сломается: диспозиция F-174 адресована одной строке одного файла, и её довод — «перенумерация внутри
файла есть видимая правка» — для межфайловых ссылок **не работает**: перенумерация правил в
`application`, `test-application-handler`, `domain-model` или `infra-capability-adapter` в диффе
ссылающегося файла не видна вообще, и ссылка молча укажет на чужое правило. Внутрифайловые четыре довод
покрывает.
Решение отложено до: человек. Развилки: (а) четыре межфайловые ссылки называют правило словами, как это
сделала S10-06 со своей; (б) только те, что ссылаются на правило, которое этот же проход правит
(`application` — задача S10-09), потому что остальные не движутся; (в) ничего — замеренного дрейфа нет ни
одного, и класс остаётся `ПРЕДСТАВИМО` даже в межфайловой форме.

## F-198 · Два шипящихся скилла несут в примере миграции ровно ту форму, которую расширенное правило про комментарии останавливает
Найдено: builder S10-07
Класс: ИЗМЕРЕНО.

```bash
rtk proxy grep -n -A1 "A check constraint's .name. is the SUFFIX" \
  plugins/adw/skills/conventions/SKILL.md plugins/adw/skills/infra-persistence/SKILL.md
# conventions:258–259 и infra-persistence:482–483 — по два комментария-строки внутри op.create_table
```

Содержимое обоих — законный non-obvious *why* (почему `name=` это суффикс, а не полное имя), но каждый
занимает **две** строки, то есть по формулировке, которую S10-07 только что распространил на
`migrations/`, это «multi-line block → stop» внутри `upgrade()`.

Что сломается: два примера, которые автор миграции копирует буквально, учат форме, за которую его
останавливает соседний скилл. Третий экземпляр семьи F-127/F-134 — «правило шире своего дерева
примеров», — и первый, где расширение правила **создало** расхождение с примером в том же каталоге, а не
обнаружило его.
Решение отложено до: человек. Развилки: (а) оба примера сжимаются до одной строки; (б) правило получает
оговорку про блок внутри списка аргументов `op.create_table`, где одна строка физически не вмещает довод;
(в) ничего — экземпляров два, и оба пока никому не соврали.

## F-199 · Отчёт исполнителя приписал форме из задачи провал, которого у неё нет, и поймал это только прогон главной сессии
Найдено: главная сессия, прогоном шага 3 протокола
Класс: ИЗМЕРЕНО.

S10-07 доложил, что строка «Проверки» отвергается инструментом: «`rtk proxy find …` is refused by the tool
itself: `rtk: rtk find does not support compound predicates`». S10-06 в тот же час доложил обратное:
падает **голый** `find`, переписанный хуком, а `rtk proxy find` работает. Прогон главной сессии решает
спор в пользу второго:

```bash
rtk proxy find plugins/adw -name '*.py' -o -name '*.sh' -o -name 'hooks.json' -o -name 'anchors.json'
# пусто, exit=0; stderr пуст
```

То есть S10-07 приписал форме из файла задачи провал своей собственной команды. Записанное в `ORIENT` §4
(F-71) верно и подтверждено третий раз.

Что сломается: ничего — и это ценность записи. Шаг 3 протокола («проверку прогоняет главная сессия, не
веря отчёту») до сих пор **подтверждал** отчёты; это первый замеренный экземпляр, где он отчёт
**опроверг**, и опроверг в направлении «исполнитель обвинил задачу». Механизм, существующий с начала
прохода, получил свой первый настоящий довод.
Решение отложено до: человек. Развилки: (а) ничего — механизм сработал, доводу место в реестре;
(б) `ORIENT` §4 получает полстроки: отчёт о **форме команды** проверяется прогоном обязательно, потому что
исполнитель не различает свою команду и команду задачи.

## F-200 · Номера строк в файлах задач устарели, и один был скопирован из задачи прошлого прохода, а не грепнут заново
Найдено: builder S10-08; главная сессия перемерила и подтвердила оба экземпляра
Класс: ИЗМЕРЕНО.

```bash
git show HEAD:plugins/adw/skills/conventions/SKILL.md | rtk proxy grep -n 'testcontainers'
# 292 (список dev-зависимостей) и 350 (глоб в [tool.mypy] module) — на :328 глоба нет
git show HEAD:plugins/adw/skills/conventions/SKILL.md | /usr/bin/awk 'NR==328'
#   fixture and helper is fully annotated: a fixture consuming the app types it `real_app: FastAPI`, a
git show HEAD:plugins/adw/skills/conventions/SKILL.md | rtk proxy grep -n 'PrimaryKeyConstraint'
# 257 — а «Границы» задачи S10-08 называли :256
```

Границы S10-08 огораживали «`conventions:328`, глоб `testcontainers.*`». Предмет живёт на `:350`; на
`:328` лежит **другое живое правило** — про паритет аннотаций, то есть сосед зоны части 1 этой же задачи.
Буквальное чтение границы огородило бы не тот предмет и оставило бы настоящий глоб неназванным.

**Происхождение номера — моё, и оно хуже простой опечатки:** `:328` скопирован из «Границ» задачи
**S09-01** прошлого прохода, где он был верен, вместо того чтобы грепнуть дерево заново. Это ровно тот
промах, против которого написана строка 1 таблицы `ORIENT` §4 («грепни дерево на артефакт, а не
вспоминай»), применённая к номеру строки, а не к перечню владельцев.

Что сломается: номера строк в файле задачи устаревают **от каждой правки волны выше по файлу** — часть 2
той же S10-08 сдвинула `:350` на +12 уже в своём диффе. Предмет опознаётся только по цитате рядом с
номером, и без цитаты исполнитель либо встаёт, либо ограждает чужое. Семья F-174 (ссылка по номеру, а не
по словам), перенесённая с номеров правил на номера строк.
Решение отложено до: человек. Развилки: (а) правило: номер строки в файле задачи **всегда** идёт вместе с
цитатой, и опознаётся по цитате; номер — удобство, цитата — предмет; (б) номеров строк в файлах задач нет
вовсе, только цитаты и грепы; (в) ничего — исполнитель опознал предмет по цитате и доложил, цена нулевая.

## F-201 · После правки поверхностей `conventions` описывает их в двух местах, и второе называет два дерева
Найдено: builder S10-08; warden S10-08 независимо, с командой
Класс: **ИЗМЕРЕНО в части текста, ПРЕДСТАВИМО в части последствий** — форма F-133. Расхождение двух мест
предъявлено командой; то, что читатель на нём спотыкается, экземпляров не имеет.

```bash
rtk proxy grep -n 'Lint and type-check cover both trees' plugins/adw/skills/conventions/SKILL.md
# 330: - **Lint and type-check cover both trees at parity** — `src` *and* `tests` — …
```

Часть 1 задачи S10-08 переписала `:275–278` на «линт и форматтер видят всё дерево, `migrations/`
включительно», а §E на `:330` по-прежнему говорит про «both trees». Формально противоречия нет — §E
говорит о **паритете** `src`↔`tests`, а не об исключительности, — поэтому builder правку не сделал:
решение F-134 постановило «одной оговоркой», и §E задача не называла.
Что сломается: читатель, искавший поверхности в §E (естественном месте — там конфиг тулчейна), получает
ответ «два дерева» и не узнаёт про третий каталог; различение окажется в §C, куда за конфигурацией не
ходят.
Решение отложено до: человек.

## F-202 · `python-style` объявляет «два предмета», а несёт третий — и объявление читается без тела
Найдено: warden S10-07
Класс: ПРЕДСТАВИМО.

`plugins/adw/skills/python-style/SKILL.md:9–10` — «Two subjects, each with a per-layer edge: what the
types look like, and who is allowed to log what», и `description` (`:3`) называет ровно эти два. Правило
про комментарии — третий предмет того же файла, и правкой S10-07 оно выросло с двух деревьев до трёх.
Что сломается: при авто-вызове в контекст попадает `description` **без тела**, поэтому предмет, которого
в `description` нет, для вызывающего не существует — та же форма, которой решена F-135, только там
абсолют был ложен, а здесь предмет пропущен. Замеренного случая, чтобы автор из-за этого правило
пропустил, нет.
Решение отложено до: человек. Развилки: (а) `description` и вводная строка называют три предмета;
(б) ничего — правило про комментарии живёт в `Hard stops`, куда приходят по стопу, а не по описанию.

## F-204 · Проверка потолка фронтматтера, которой пользуется этот проход, считает не ту величину, которую ограничивает платформа
Найдено: builder S10-09; главная сессия перемерила
Класс: ИЗМЕРЕНО.

```bash
/usr/bin/sed -n '3,4p' plugins/adw/skills/application/SKILL.md | /usr/bin/wc -c        # 634 (632 в -m)
uv run --with pyyaml python -c "…yaml.safe_load…; len(description)+len(when_to_use)"   # 604
```

Форма `sed -n '3,4p' … | wc -c` считает **байты двух строк файла** — вместе с именами полей
(`description: `, `when_to_use: `) и переводами строк, — а ограничение платформы наложено на сумму
**значений** двух полей. Разница здесь 30 в запас, поэтому вывод «меньше 1536» остаётся верным, и он
верен и до правки (460 против ~430).

Что сломается: знак ошибки не постоянен. На многобайтных тире и типографских кавычках байты и символы
расходятся заметно, а на скилле с длинным `name`/`paths` та же форма, применённая к более широкому
диапазону строк, завысит ещё сильнее — и «потолок пересчитан командой» будет означать пересчитанный **не
тот** потолок. Это семья F-146 (команда мерит не тот предмет, что утверждение рядом), и её первый
экземпляр, где ошибающаяся команда стоит в файле задачи **этого** прохода.
Решение отложено до: человек. Развилки: (а) форма проверки становится замером значений полей (одна строка
на `python` с `yaml.safe_load`), и `meta-skill-author` её называет; (б) остаётся `sed | wc -c` с явной
оговоркой «завышает, годно как верхняя оценка»; (в) ничего — запас 900+ символов, и ни один скилл к
потолку не приближался.

## F-205 · У модульной функции, которую новое правило `domain-model` назвало домом предиката, нет контракта упаковки — и два указания на одном шаге исключают друг друга
Найдено: builder S10-10; главная сессия перемерила обе цитаты
Класс: ИЗМЕРЕНО.

```bash
sed -n '150,158p' plugins/adw/skills/architecture/SKILL.md
# Carve-out 3 — wildcard only class-modules … A module whose public name is a bare object rather than a
# class … is **not** wildcarded into its package `__init__` … reach it by explicit relative import
sed -n '310,311p' plugins/adw/skills/domain-model/SKILL.md
# After writing the module, follow `architecture` to add the subpackage `__init__.py` re-export
# line and append to its `__all__`.
```

Плюс `architecture:116` — «**One class per module file**», и `:168` — «These are the **only** exceptions,
and the carve-out is by exact file path». То есть модуль с публичной **функцией** не покрыт ни правилом
имени, ни перечнем исключений, а два указания на одном шаге противоречат друг другу: `Package wiring`
велит положить вайлдкард и `__all__`, карве-аут 3 вайлдкард запрещает.

Что сломается: автор, исполнивший новый случай F-144, выберет сам, и в двух проектах выйдет разное. И
**создало это расхождение решение F-144** — до него формы «модульная функция в домене» в каталоге не
было вовсе, поэтому и контракта упаковки ей не требовалось. Второй экземпляр за проход, где правка
правила создала расхождение с соседним файлом, а не обнаружила его (первый — F-198).
Решение отложено до: человек. Развилки: (а) `architecture` получает четвёртое named-исключение или
расширение карве-аута 3 на модуль с публичной функцией — то есть правку в файле, которого решение F-144
не касалось; (б) `domain-model` в новом случае сам говорит, как модуль достаётся (явным относительным
импортом, без вайлдкарда), и тогда `architecture` не трогается вовсе; (в) ничего — экземпляр один и он в
пробе не воспроизведён.

## F-206 · Записанный сигнал отзыва и шипящийся расходятся одним словом: реестр говорит «`Design` спеки», дерево — «spec»
Найдено: warden S10-09
Класс: ПРЕДСТАВИМО.

Диспозиция F-139 и тело записи называют сигнал через «`Design` спеки»; шипящееся условие отзыва, внесённое
S10-09, говорит «whose spec does not tie the write and the returned data into one call». Расхождение
**обосновано и правки в дереве не требует** — довод замерен:

```bash
rtk proxy grep -rn 'Design' plugins/adw/skills/     # пусто во всех тридцати скиллах
rtk proxy grep -rln 'Design' plugins/adw/           # только agents/ и commands/ и templates/spec.md
```

`Design` — имя секции артефакта цикла, и внос его в тело скилла завёл бы в портируемое ядро знание о том,
кто скилл вызывает, плюс второй дом структуры спеки. Довод warden'а сильнее довода исполнителя: условие
отзыва обязано быть **отрицанием** условия самого исключения, а исключение двумя строками выше говорит
«legal only where **the spec** ties the two together», — вставь туда `Design`, и абзац назвал бы связь
двумя разными носителями.

Что сломается: сверка на строку. Кто позже будет проверять, исполнено ли F-139, по `grep 'Design'` в
скиллах не найдёт ничего и сочтёт условие ненаписанным — то есть ровно тот класс, что F-68 (ссылка, которая
не открывается), только наоборот: открывается не ссылка, а её цель под другим именем.
Решение отложено до: человек. Развилки: (а) диспозиция F-139 получает полстроки о том, что в ядре носитель
связи называется «spec», потому что имени секции там быть не может; (б) ничего — расхождение объяснено в
этой записи, и она находится грепом по F-139.

## F-208 · Два требования моего файла задачи были неисполнимы буквально, и исполнитель обошёл оба верно
Найдено: builder S10-11
Класс: ИЗМЕРЕНО.

**Первое.** «Что скажет warden» задачи S10-11 требовало: «замер приведён числом **с источником** — „73
passed“ и **чей это проход**». Исполнить буквально значит внести в шипящийся скилл «adversarial-проход
evaluator'а change 002 второй пробы» — роль, номер изменения и имя нашей пробы, то есть ровно тот словарь,
который запрещает правило «A skill must not know what invokes it». Исполнитель назвал источником
**эксперимент** («on a project whose suite was 73 tests, flipping only the `Table`'s range bound…»), и это
верно: решение человека по F-189 наименования прохода не требовало — это добавка автора задачи.

**Второе.** Формулировка «„никогда“ сужается **до одноколоночных**», применённая ко всем четырём видам
(pk, FK, unique, index), создала бы новое противоречие в том же файле: конвенция `pk_%(table_name)s`
интерполирует только имя таблицы (замер F-171), а составной primary key junction-таблицы объявляется через
`primary_key=True` вовсе без `name=` (`infra-persistence`, раздел про junction). Исполнитель сузил ровно
там, где бьёт замер F-176 — unique и index, — оставив pk и FK безусловными.

Что сломается: оба требования — мои, и оба того класса, что F-138 (задача просит больше или иначе, чем
решено). Первое опаснее: оно требовало нарушить стоящее правило каталога, и warden, читающий чек-лист
буквально, **отклонил бы верную работу** — то есть F-154 наоборот. Ловится это только тем, что исполнитель
открывает правило, а не исполняет пункт.
Решение отложено до: человек. Развилки: (а) правило: пункт «Что скажет warden» проверяется против стоящих
правил каталога так же, как блок «Проверка» — против Deliverables (расширение решения раунда 3 по
F-151/F-154 на третью секцию); (б) ничего — исполнитель поймал оба и доложил, цена нулевая.

## F-210 · Уточняющее решение жило только в файле задачи, и реестр противоречил шипящемуся дереву
Найдено: builder S10-12
Класс: ИЗМЕРЕНО. `rtk proxy grep -n 'test-author' plan/FINDINGS.md` до этой правки давал только строки
диспозиций раунда 5, где домом яруса назван `implementer.md`; уточняющее решение существовало **только** в
`plan/S10-12-substrate-belongs-to-the-skeleton.md`.

Что сломалось бы: читатель, грепающий реестр по F-180, получает другой дом и либо переносит ярус в
имплементера — прямо против `implementer.md:3`, — либо записывает расхождение как промах исполнителя.
**Это третий экземпляр F-113 за два дня**, и первый, где решение не отсутствовало вовсе, а лежало не в
доме: протокол требует диспозицию в реестре **раньше**, чем написан ссылающийся файл задачи, и я написал
задачу, сославшись на решение, которого в реестре не было.
Исправлено немедленно секцией выше — ровно тем способом, которым протокол это и предписывает.
Решение отложено до: человек. Развилки: (а) ничего — промах поймал исполнитель, цена одна секция;
(б) шаг протокола получает половину строки: решение, принятое **в ходе** прохода, ложится в реестр до того,
как правка уходит в диспатч, а не после.

## F-211 · Заголовок раздела называет один артефакт, а раздел перечисляет пять классов субстрата
Найдено: builder S10-12
Класс: ИЗМЕРЕНО. Заголовок `implementer.md` — «On the project's first change, the `Makefile` is part of the
substrate»; содержание после правки — оболочка, wiring, `Makefile`, конфигурация тулчейна, бутстрап
Alembic, плюс различение владения манифестом.
Что сломается: ничего сегодня — раздел был шире заголовка и до правки (оболочка и wiring в нём уже
стояли). Builder заголовок не менял сознательно: переименование раздела шипящейся роли — решение человека,
а не правка по дороге.
Решение отложено до: человек.

## F-212 · Проза роли называет предзагрузку двух ролей словами — верно сегодня, и сигнала об устаревании нет
Найдено: warden S10-12
Класс: ПРЕДСТАВИМО.

`test-author.md` после правки S10-12 говорит про `skills:` обеих ролей словами: у имплементера
`architecture` предзагружен рядом с `conventions`, у test-author пара — `conventions` + `test-principles`.
Warden сверил с фронтматтерами обоих файлов: **сегодня верно**. Формулировка предписана Deliverables задачи
дословно, поэтому нарушением не является.
Что сломается: добавят `adw:architecture` в `skills:` у test-author — абзац станет ложным, и ни один сигнал
об этом не сработает. Это связка **ядра с адаптером**: проза (ядро) утверждает факт о фронтматтере
(адаптер), а красная линия 6 держит их раздельно именно затем, чтобы адаптер можно было переписать за вечер.
Решение отложено до: человек. Развилки: (а) довод переписывается без имён скиллов — «роль, у которой
архитектурный контекст предзагружен», без перечня; (б) ничего — экземпляр один, и правка фронтматтера роли
проходит через человека.

## F-215 · Третий дефект моих файлов задач за волну: требование поставить в шипящуюся роль ссылку по номеру шага
Найдено: builder S10-13
Класс: ИЗМЕРЕНО.

`plan/S10-13-…md` в части 1 требовало назвать предусловие как «коммит этот делает **шаг 5** команды». Это
межфайловая ссылка по ординалу внутри шипящегося файла — ровно класс, против которого решение того же дня по
F-174, и ни одна роль сегодня так не ссылается:

```bash
rtk proxy grep -rn 'step [0-9]' plugins/adw/agents/    # пусто
```

Исполнитель предусловие назвал словами («the change cycle commits it immediately before dispatching you») и
ординал не поставил, назвав правило.

Что сломается: ничего сегодня. Значение записи — в счёте: **три** файла задач этой волны несли требование,
неисполнимое буквально (F-200 — устаревший номер строки, F-208 — два требования против стоящих правил, эта
— ординал против решения того же дня), и все три поймал исполнитель, открывший правило вместо исполнения
пункта. Это довод к развилке (а) F-208: секция «Что скажет warden» проверяется против стоящих правил так же,
как блок «Проверка» — против Deliverables.
Решение отложено до: человек — той же развилкой, что F-208.

## F-217 · Warden доложил, что половина F-184 не закреплена ни за одной задачей — и это верно про файлы задач, но не про таблицу
Найдено: warden S10-13; главная сессия уточнила предмет
Класс: ИЗМЕРЕНО.

```bash
rtk proxy grep -n -i 'watchdog\|no progress' plan/PLATFORM.md        # пусто — половина диспозиции не исполнена
rtk proxy grep -ln 'PLATFORM' plan/S10-*.md                          # ни одного файла задач
rtk proxy grep -n 'S10-04' plan/INDEX.md                             # строка есть: «PLATFORM: пять фактов, что уже замерены»
```

Warden прав по факту: замеренных фактов в `PLATFORM.md` нет, и ни один **написанный** файл задачи его не
называет. Уточнение: закрепление существует — строка `S10-04` в таблице `INDEX.md`, — но её файл на момент
ревью ещё не написан, поэтому греп по `plan/S10-*.md` его не видит.
Что сломается, если не назвать различие: следующий читатель решит либо что задачи нет, либо что она есть и
исполнена. **Настоящий сигнал здесь другой и он про порядок работ:** шипящаяся команда уже ссылается на
поведение платформы, которого в её каноне нет, и это состояние продержится до исполнения S10-04.
Решение отложено до: человек. Развилки: (а) ничего — S10-04 в таблице, и порядок волны её несёт;
(б) правило: задача, чей файл ещё не написан, не считается закреплением для warden'а, и такие остатки
называются в диспозиции прохода явным перечнем.

## F-218 · Мутационный проход создал асимметрию: обязательный раздел вердикта читает дифф только по `tests/`
Найдено: warden S10-13
Класс: ПРЕДСТАВИМО.

Раздел «Edits to `tests/**` after baseline» обязателен и читает `git diff <baseline>..HEAD -- tests/`. Мутации
после S10-13 ложатся в `src/`, и забытая мутация не попадает ни в один раздел вердикта — её ловит только
человек на приёмке, тем самым чтением диффа, на которое ссылается `## What you cannot do`.
Замеренных экземпляров оставленной мутации нет, и форма требует подтверждения `git status --porcelain`
пустым, чего сегодня достаточно.
Решение отложено до: человек. Сигнал к переоткрытию назван warden'ом: **первая** мутация, найденная
оставленной в дереве на приёмке.

## F-221 · Формулировка, убранная из шаблона, живёт третьим экземпляром в роли красной фазы — и там она, возможно, законна
Найдено: builder S10-19
Класс: ИЗМЕРЕНО.

```bash
rtk proxy grep -rn 'say whether\|would catch it' plugins/adw/
# ровно одно совпадение после правки S10-19: agents/test-review.md:67
# «…if it were the first — and say whether any test would catch it.»
```

До S10-19 таких мест было два — шаблон вердикта и эта роль; шаблон приведён к форме «применить, прогнать,
откатить», роль осталась с рассуждением.

**И это, скорее всего, верно, а не дефект.** У `test-review` красная фаза: реализации ещё нет, мутацию
построить не из чего, и рассуждение — единственная доступная ему форма. Дефект не в форме, а в том, что
**причина расхождения нигде не названа**: два файла дословно расходятся в одном месте и совпадают в
соседнем (перечисление слабых утверждений идентично), и самый дешёвый способ вернуть рассуждение в вердикт —
прочитать формулировку у `test-review` и перенести обратно. F-214 закрыта в файле, **с которого копируется
артефакт**, и осталась открытой в файле, **с которого копируют формулировку**.
Решение отложено до: человек. Развилки: (а) `test-review.md` получает оговорку в одну строку — почему у
него форма другая (реализации нет, мутацию не построить), и тогда расхождение читается как намеренное;
(б) ничего — роли разные, и совпадения текста никто не обещал.

## F-222 · Число об идущей волне устаревает до коммита по устройству: 9 → 12 за девять минут
Найдено: builder S10-01
Класс: ИЗМЕРЕНО.

Файл задачи S10-01 велел назвать в `ORIENT` §4 число экземпляров, которые правило F-136 поймало за проход,
и сказал «восемь». Пока задача исполнялась, счёт рос: 22:57 — девять, 22:58 появился `plan/S10-19`
(«десятый раз»), 23:02 `plan/S10-03` («одиннадцатый»), 23:05 `plan/S10-04` («двенадцатый»). Времена —
`ls -lT plan/S10-*.md`, счёт — грепом по пометкам в файлах задач.

Новизна против F-82: там число устаревало от **последующих правок того же диффа**, и лечилось пересчётом
последним действием. Здесь оно устаревает от **параллельных задач той же волны**, то есть пересчёт
последним действием не помогает — к моменту коммита волна успевает вырасти снова. Любое число об идущей
волне, записанное задачей этой волны, ложно по устройству.
Что сломается: не текст — метод. Правило «пересчитай последним действием» верно и недостаточно, когда
предмет счёта пишется параллельно.
Решение отложено до: человек. Развилки: (а) число об идущей волне не пишется вовсе — пишется команда,
которой его получат, и дата; (б) число пишется, но с датой и временем замера рядом; (в) ничего.

## F-223 · Два дефекта файла задачи S10-01, оба поймал исполнитель
Найдено: builder S10-01
Класс: ИЗМЕРЕНО.

**Первый — число в заголовке секции.** «Шесть правок» при семи нумерованных пунктах плюс отдельный абзац:
`rtk proxy grep -c '^\*\*[0-9]\.' plan/S10-01-orient-check-table.md` → **7**. Исполнитель, считающий по
заголовку, останавливается на шестом и не ставит дату ревизии (пункт 7). Это F-151/F-154 дословно — число в
файле задачи устарело от правок того же диффа, и перечитывания последним действием я не сделал.

**Второй — команда счёта считает не тот предмет.** Файл задачи давал
`rtk proxy grep -c 'правило F-136' plan/S10-*.md`: с глобом `-c` печатает **по-файловые** счётчики, а не
итог, и паттерн матчит ещё и строку-инструкцию в самом файле задачи — `grep -o` той же формы даёт 13 против
12 настоящих экземпляров. То есть команда счёта внутри задачи **про то, что проверка должна мерить тот
предмет**, мерила не тот.

Что сломается: четвёртый и пятый дефекты моих файлов задач за проход (после F-200, F-208, F-215), и оба —
в задаче, чей предмет ровно этот класс. Ловится это по-прежнему только тем, что исполнитель открывает
правило вместо исполнения пункта.
Решение отложено до: человек — той же развилкой, что F-208.

## F-224 · Абзац §4 «промахи ловит исполнитель» устарел по соотношению, и устарел от этого же прохода
Найдено: builder S10-01
Класс: ИЗМЕРЕНО. `ORIENT` §4 объясняет ценность таблицы так: «Чем это окупается, видно по тому, кто промахи
ловил: **почти все — исполнитель**». За этот проход **двенадцать** промахов поймал автор — до диспатча,
правилом F-136, — против семи, пойманных исполнителями.
Что сломается: читатель берёт из §4 вывод «правило нужно ради исполнителя», тогда как замер говорит
обратное: самый дешёвый ловец — автор, если он гоняет проверку до отправки. Builder абзац не тронул
сознательно: его правка в задаче не названа, и он поставил свой замер рядом.
Решение отложено до: человек. Развилки: (а) абзац переписывается по замеру этого прохода; (б) ничего —
соотношение может вернуться, когда правило станет привычкой.

## F-226 · Оговорка про каскад спеки покрывает промпты 003 и 004, а три места ссылаются на неё как на «003–005»
Найдено: builder S10-16
Класс: ИЗМЕРЕНО. `plan/PROBE2-PROMPTS.md:13` дословно: «Промпты 003 **и 004** написаны до того, как 001 и
002 существуют». Промпта 005 в файле нет вовсе, и тот же файл это признаёт («Пятое изменение промпта пока
не имеет»), а строка 005 в таблице есть.

Ссылались на неё как на «003–005» **тело F-193 и файл задачи S10-16** — то есть я, дважды, и второй раз
скопировав первый.
Что сломается: читатель, доверившийся формулировке «003–005», решит, что защита от каскада распространена и
на пятое изменение. На деле его промпт ещё не написан, и когда появится, оговорка его по букве не покроет —
а он самый дальний от живой спеки и самый уязвимый к каскаду.
Решение отложено до: человек. Развилки: (а) оговорка говорит «каждый промпт, написанный до того, как
существует изменение перед ним» — тогда она покрывает и будущий 005; (б) ничего — 005 напишется вместе со
своей оговоркой.

## F-227 · Третья колонка таблицы прогнозов растёт быстрее, чем читается
Найдено: builder S10-16
Класс: ИЗМЕРЕНО. После приведения строки 002 к факту она стала 119 символов при потолке прозы этого файла
104 и прежнем максимуме строки таблицы 105. Markdown-строку таблицы перенести нельзя, а линтера длины на
`.md` в репозитории нет (`.pre-commit-config.yaml` несёт `ruff-check`, `ruff-format`, `strip-ai-trailers`).
Что сломается: ничего сегодня. Но третья колонка — это место, куда каждое приведённое к факту изменение
дописывает, и к пятой строке форма «таблица» перестанет читаться раньше, чем перестанет быть верной.
Решение отложено до: человек.

## F-228 · Предусловие мутационного прохода живёт в роли и не живёт в шаблоне
Найдено: warden S10-19
Класс: ПРЕДСТАВИМО. `evaluator.md` несёт предусловие («имплементация уже закоммичена; если нет — не
мутировать, сказать в вердикте и рассуждать»); `templates/verdict.md` называет только две причины «не
построить мутацию» — поведение вне дерева и критерий, проверяемый только вживую. Замеренного случая нет,
evaluator читает оба файла, и дописывать это в шаблон было бы превышением объёма S10-19.
Решение отложено до: человек. Запись существует, чтобы «ничего» было названо явно, а не по умолчанию.

## F-229 · Файл задачи может внести пункт без основания вообще, и секция «Происхождение» этого не показывает
Найдено: warden S10-01
Класс: ИЗМЕРЕНО.

Седьмой пункт `plan/S10-01-orient-check-table.md` не назван ни в «Происхождении» (там десять строк решений
человека), ни цитатой решения в теле, а по содержанию — развилка (б) записи F-203, на тот момент открытой.
Warden это поймал и отклонил работу, назвав правило: диспозиция находки — решение человека и приходит своим
коммитом.

**Тонкость, которую он назвал точнее меня:** файл задачи цитату **не подделал** — он нигде не пишет «по
решению человека» про этот пункт. Это не F-68 (ссылка, которая не открывается), а вещь мягче и потому хуже
ловимая: пункт вошёл **без основания вообще**. У задачи есть два класса пунктов — исполнение решения и
инициатива автора, — и сегодня они выглядят в файле одинаково.

Ни warden'ская проверка, ни чек-лист «Что скажет warden» этого не ловят: чек-лист спрашивал только «число
посчитано в этой волне, а не перенесено из F-203», то есть проверял **свежесть числа** у пункта, чьё
**право на существование** никто не проверял.

Побочно тем же ревью **подтверждена F-222 вживую**: пока билдер работал, число выросло с 12 до 13 —
тринадцатая пометка пришла из параллельной `plan/S10-16`, и приложенная к абзацу команда уже расходилась с
числом над ней.
Что сломается: прецедент «файл задачи может исполнить развилку, которую человек ещё не выбрал» — это
механика «11 задач → 64», где каждая задача сама выносила решение по находке, которую нашла. Цена отката
здесь была десять строк; цена приёма — прецедент.
Решение отложено до: человек. Развилки: (а) каждый пункт «Задачи» несёт рядом строку диспозиции, на которой
стоит, и пункт без такой строки в файл не входит; (б) секция «Происхождение» перечисляет пункты, а не
только строки диспозиций, — тогда расхождение видно счётом; (в) ничего — warden поймал с первого раза.

## F-230 · `git add <каталог>` в волне с параллельными задачами закоммитил чужую незаревьюенную и одну отклонённую работу
Найдено: главная сессия, сверкой `git show --stat` со своим намерением
Класс: ИЗМЕРЕНО.

Коммит `042df23` задумывался как одна запись в реестр (F-229). Команда была `git add plan/`, и в него уехало:

```bash
git show --stat 042df23 | rtk proxy grep 'PLATFORM\|ORIENT\|FINDINGS'
# plan/FINDINGS.md   |  28 +++    ← намеренно
# plan/ORIENT.md     |  30 +++    ← работа S10-01, которую warden ОТКЛОНИЛ
# plan/PLATFORM.md   | 151 +++    ← работа S10-04, до ревью вообще
```

То есть шаг 4 протокола («warden `ПРИНЯТЬ` + проверка зелёная → коммит») нарушен дважды одной командой, и
худшая половина — **закоммичена отклонённая работа**, поверх которой в ту же минуту шёл второй круг.

Класс родственен F-99, но зеркален ему: там `git status` давал **ложный красный** на честной работе соседа,
здесь `git add <каталог>` даёт **ложный зелёный** — чужая работа уезжает в коммит молча, и сообщение
коммита о ней не говорит ни слова. F-99 лечилась тем, что **проверка** предъявляет свои пути; эта сторона —
что **коммит** ставит свои пути, — не была названа нигде.

Что сломается: сообщение коммита перестаёт описывать свой дифф, ревью обходится, а откат отклонённой работы
становится правкой поверх коммита вместо невыполненного коммита. Здесь цена оказалась мала — ничего не
запушено, ревью S10-04 запущено следом, второй круг S10-01 идёт поверх, — но она мала случайно.
Решение отложено до: человек. Развилки: (а) шаг 4 протокола получает половину строки: коммит ставит
**свои пути**, `git add <каталог>` и `git add -A` в волне запрещены — симметрично правилу шага 1 про дифф;
(б) ничего — промах поймала сверка `--stat`, и она в шаге 4 подразумевается.

## F-231 · Диспетчерское сообщение сослалось на диспозицию, которой в реестре нет — F-68 на месте, которое никто не читает
Найдено: builder S10-01, второй круг
Класс: ИЗМЕРЕНО.

```bash
rtk proxy grep -n '^- \*\*F-222' plan/FINDINGS.md      # пусто — строки диспозиции нет
rtk proxy grep -n 'F-222' plan/FINDINGS.md             # одна строка: заголовок самой записи
```

Промпт второго круга S10-01, написанный главной сессией, гласил: «это записано как **F-222**… диспозиция уже
в реестре». Записана **находка**, а решения по ней нет — тело F-222 кончается «Решение отложено до:
человек» с тремя развилками.

**Новизна против F-68 существенна.** Там ссылка на несуществующее решение стояла в **файле задачи** —
артефакте, который читают исполнитель и warden, который лежит в дереве и попадает в дифф. Здесь она в
**устном довороте диспатча**: его не читает никто после, он не версионируется и в ревью не попадает. То
есть единственное место, где промах ловится, — внимательность исполнителя, и здесь она сработала.
Исполнитель не остановился, и это защитимо: он разобрал по частям, на чём стоит каждая строка его правки, и
показал, что ни одна не держится на отсутствующем ruling'е.
Что сломается: доворот диспатча — канал, в котором главная сессия может сказать что угодно без следа.
Правило «задача самодостаточна, пересказ в промпте — второй источник правды» существует именно против
этого, и здесь я его нарушил сам.
Решение отложено до: человек. Развилки: (а) доворот при повторном диспатче ограничен вердиктом warden'а и
ссылкой на строку реестра — всё остальное пишется в файл задачи, где остаётся следом; (б) ничего —
исполнитель поймал, и правило про самодостаточность уже есть.

## F-232 · Ruling, закрывающий F-203, сам несёт число об идущей волне — и оно устарело дважды за час
Найдено: builder S10-01, второй круг; главная сессия перемерила
Класс: ИЗМЕРЕНО. Диспозиция F-203 в секции «принятые в волне 2» говорит «не четыре экземпляра, а
**двенадцать**». Замер:

```bash
rtk proxy grep -h '^#.*правило F-136' plan/S10-*.md | wc -l   # 14
```

Двенадцать → тринадцать (замерил warden S10-01) → четырнадцать, за один час.

**F-222 сформулирована про тексты задач и правок, а её механизм ловит и сам реестр решений.** И по решению
F-225 («реестр задним числом не переписывается») это число останется ложным **навсегда**: следующий
читатель ruling'а возьмёт «двенадцать» как замер. То есть два решения одной волны столкнулись — одно
запрещает править число, другое объясняет, почему оно ложно.
Что сломается: не текст, а доверие к числам в секциях решений. Развилка, которой у F-222 ещё нет, теперь
имеет второй адресат: не только файлы задач, но и диспозиции.
Решение отложено до: человек. Развилки: (а) правило «число об идущей волне не пишется» распространяется на
диспозиции, и в этой конкретной строке число заменяется командой — это правка ruling'а того же дня, то есть
не «задним числом» в смысле F-225, а в пределах прохода; (б) ничего — рядом стоит эта запись, и греп по
F-203 её найдёт.

## F-233 · У второго круга нет «до»-состояния, на которое ссылается «Проверка» задачи
Найдено: builder S10-01, второй круг
Класс: ИЗМЕРЕНО. `git diff --stat -- plan/ORIENT.md` на втором круге дал `3 insertions, 13 deletions` вместо
записанного в задаче «до: пусто»: работа первого круга ушла в HEAD коммитом `7e414ae` (по F-230), и строки
«до: пусто», «до: 0», «`:180`» описывают дерево, которого больше нет.

Что сломается: «Проверка» замерена как исполнимая ровно один раз — на первом круге. При отклонении
warden'ом её «до» надо перечитывать как «до **первого** круга», и ни один сигнал об этом не срабатывает:
числа «после» сошлись бы и в случае, когда исполнитель второго круга принял бы чужой коммит за собственную
работу. Здесь совпало лишь потому, что исполнитель сверил номера строк и назвал расхождение сам.
Решение отложено до: человек. Развилки: (а) при повторном диспатче «до» переопределяется явно — «состояние
на момент отклонения», и это половина строки в шаге 4 протокола; (б) ничего — второй круг редок, а
исполнитель расхождение назвал.

## F-234 · Преамбула датированного раздела `PLATFORM.md` не покрывает два своих пункта из девяти
Найдено: warden S10-04
Класс: ИЗМЕРЕНО. `plan/PLATFORM.md:441–444` — заголовок раздела «Побочное, что попало в замер» несёт
«**Дата: 2026-07-29**» и «Всё ниже — ЗАМЕРЕНО в тех же прогонах». При этом пункт 8 помечен
`ИЗ ДОКОВ, не проверено` — **до** этой волны, — а пункт 9 (`automode-unavailable`) добавлен S10-04 с датой
2026-08-05.
Что сломается: файл держится на конвенции «даты у файла нет: её несёт каждый раздел отдельно»
(`PLATFORM.md:5`), и читатель, берущий дату и класс с шапки раздела, получит для двух пунктов чужие.
Исполнителю правка была запрещена Границами («не чинить найденное по дороге»), и он доложил её сам.
Решение отложено до: человек. Развилки: (а) преамбула теряет «Всё ниже — ЗАМЕРЕНО в тех же прогонах» и
дату, а класс с датой несёт каждый пункт — как уже делают восьмой и девятый; (б) одиночные факты про
инструменты получают свой раздел, и список остаётся датированным замером 2026-07-29; (в) ничего.

## F-235 · Перенос по F-159 внёс в §13 три факта, тогда как решение называет один
Найдено: warden S10-04
Класс: ИЗМЕРЕНО. Решение по F-159 говорит: урок про источник маркетплейса — «**единственный** факт §13,
которого там нет». В дифф пришли ещё два — «`1.1.1` не была установлена никогда» и «устаревшие копии
`update` не убирает, третий раз».

**Warden не отклонил, и его довод стоит записать:** оба побочных факта лежат **в теле той же находки**
(«два побочных факта того же замера»), которую решение закрывает; ни один не несёт собственного решения, а
третий явно помечен как воспроизведение уже записанного факта §13. Расширил решение **молча файл задачи**
(мой), а не исполнитель.
Что сломается: это F-229 в мягкой форме — задача шире решения, но в пределах тела закрываемой находки.
Если человек хочет, чтобы прецедент F-229 покрывал и такой случай, это его экземпляр.
Решение отложено до: человек.

## F-236 · Решение по F-203 противоречит себе по объёму и называет число, которого в принятом тексте нет
Найдено: warden S10-01, второй круг
Класс: ИЗМЕРЕНО. Диспозиция F-203 в одной строке санкционирует «**половину строки**» (цитируя развилку (б))
и тут же оставляет «**абзац** … не четыре экземпляра, а двенадцать». Второй круг выбрал полстроки и снял
число — выбор в пользу меньшего объёма превышением не является.
Что сломается: человек оставил абзац, сославшись на замер, которого в принятом тексте больше нет; и число
«двенадцать» в самом ruling'е ложно (F-232, сегодня 14). То есть одна строка решения несёт два дефекта
разом — объём и число.
Решение отложено до: человек. Развилки: (а) строка ruling'а переписывается в пределах прохода — «полстроки,
без числа, счёт командой»; (б) ничего, и рядом стоят F-232 и эта запись.

## F-237 · Watchdog убил два диспатча волны, и правило, положенное этой же волной, немедленно окупилось
Найдено: главная сессия, применением правила `build.md` к своему же прогону
Класс: ИЗМЕРЕНО.

Два диспатча S10 вернулись с `Agent stalled: no progress for 600s (stream watchdog did not recover)` —
S10-15 и S10-02, оба на фразе «Now the full «Проверка» section», то есть **после** правок и до отчёта.
Пункт про убитый диспатч лёг в `build.md` задачей S10-13 **за час до этого**, и он говорит: сначала прочитай
дерево, правки убитого агента остаются на диске. Прочитал:

```bash
git -C ~/Projects/adw-rooms log --oneline -1     # f7032e0 — S10-15 успела закоммитить, дерево чистое
git status --short                                # M plan/ORIENT.md, M plan/FINDINGS.md — правки S10-02 на диске
```

**Обе работы оказались целы**, погибли только отчёты: у S10-02 все семь текстовых проверок и главная —
прогон самой команды открытых записей — зелёные; у S10-15 коммит на месте. Слепой ре-диспатч затёр бы
правки S10-02 и продублировал коммит S10-15.

Что это значит: **второй замер класса F-184 за два дня**, и первый, где правило уже существовало. Стоимость
правила измерена ровно один раз и сразу окупилась двумя спасёнными работами.
Побочно: watchdog бьёт по 600 секундам **бездействия потока**, а не по общей длительности — S10-05 работала
2187 секунд и вернулась нормально.
Решение отложено до: человек. Развилки: (а) ничего — правило есть, оно сработало, запись это фиксирует;
(б) в `build.md` добавляется наблюдение про 600 секунд бездействия против общей длительности, потому что
разница между ними меняет то, чего ждать.

## F-238 · Тело F-187 опровергнуто экспериментом: обе пробы на directory-пути, и `1.1.0` видят обе
Найдено: prober S10-05; главная сессия перемерила
Класс: ИЗМЕРЕНО.

F-187 утверждала: «*Первая проба* — установка через кэш. Активная копия приколота к SHA `89ac6e8c8ea9`… и
её `plugin.json` не несёт ключа `version` вовсе». Замер говорит обратное:

```bash
ls -l ~/Projects/adw-probe/.claude/settings.json
# -rw-r--r-- 249 Jul 30 10:15 — файл есть, mtime совпадает с installedAt записи реестра
rtk proxy grep -c 'extraKnownMarketplaces' ~/Projects/adw-probe/.claude/settings.json   # 1
```

Сессия из `~/Projects/adw-probe` сообщает `path` в **рабочее дерево** и отвечает `FILTERWARNINGS=PRESENT`,
хотя в кэш-копии `89ac6e8c8ea9` этой строки нет вовсе. То есть у первой пробы есть свой
`extraKnownMarketplaces`, дословно совпадающий со второй, и **обе** сегодня на directory-пути.

Отсюда падает и вывод F-187 «`1.1.0` сегодня не видит никто»: обе сессии сообщают `version: 1.1.0`.
Что сломается: запись, на которой стоит понимание «доехала ли правка до пробы», описывала механизм, которым
пробы не подключены. Ошибка была в том, что реестр установок читался как единственный источник, а
проектный конфиг — нет.
**И это первый случай в проходе, когда эксперимент опроверг тело записи, а не уточнил число.**
Решение отложено до: человек. Развилки: (а) тело F-187 не правится (реестр задним числом не переписывается),
и опровержение живёт этой записью плюс разделом `PLATFORM.md`, куда его положила S10-05; (б) F-187
переоткрывается как «частично неверна».

## F-239 · Восьмой дефект моих файлов задач: ссылка на раздел, которого S10-04 не клала
Найдено: prober S10-05
Класс: ИЗМЕРЕНО. «Читать сначала» задачи S10-05 велит открыть «`plan/PLATFORM.md` §13 и раздел про два пути
подключения, **положенный S10-04**». Такого раздела в диффе `042df23` нет, и файл задачи S10-04 его не
заказывал: её пять фактов — F-14, F-129, F-159, F-184, F-195, и путей подключения среди них нет.
Класс F-68 — ссылка, которая не открывается. Исполнитель не остановился и объяснил, почему: диспозиция
F-187 и строка «Проверки» называют предмет прямо, так что существо задачи от несуществующей ссылки не
зависело.
Что сломается: восьмой экземпляр за проход (после F-200, F-208, F-215, F-223 ×2, F-231, F-235), и все
восемь поймали исполнители. Соотношение из F-224 продолжает расти в ту же сторону.
Решение отложено до: человек — той же развилкой, что F-208 и F-229.

## F-240 · Шапка `PLATFORM.md` пришпилена к `2.1.220`, платформа сегодня `2.1.222`
Найдено: prober S10-05
Класс: ИЗМЕРЕНО (`claude --version` → `2.1.222 (Claude Code)`; шапка файла называет `2.1.220`).
Исполнитель шапку не правил — она вне Deliverables, — и дал версию каждому своему новому разделу отдельно.
Что сломается: шапка обещает версию всему файлу, а разделы теперь называют две разные. Правило перепроверки
замеров держится ровно на этом поле, и читатель, берущий версию с шапки, получит для двух разделов чужую.
Родственно F-234 (преамбула раздела не покрывает свои пункты) — тот же класс на этаж выше.
Решение отложено до: человек. Развилки: (а) шапка перестаёт называть версию, и её несёт каждый раздел — как
уже делают самые свежие; (б) шапка обновляется до `2.1.222` с датой, и старые разделы сохраняют свои.

## F-241 · `skills count: 48` в поле `init` против 30 в дереве — число законное, ярлык обманывает
Найдено: warden S10-05
Класс: ИЗМЕРЕНО.

```bash
rtk proxy grep -n 'skills count' plan/PLATFORM.md      # 1353: skills count: 48
ls -d plugins/adw/skills/*/ | wc -l                     # 30
rtk proxy grep -n '48' plan/INDEX.md | head -3
# 35: Каталог откатан к 48 дослитийным скиллам …   40: R02 | слияния 48 → 30
```

Число — **дословное поле `init`** сессии, и как дословный вывод оно законно: легенда требует именно вывода.
Обманывает совпадение: 48 в этом репозитории — исторический размер каталога **до** пересборки в 30, и
читатель, приняв поле сессии за счёт скиллов плагина, уносит откат на поколение назад.

**Два прочтения, и различать их важно.** (а) Поле считает **все** скиллы, доступные сессии, — плагинные,
проектные и встроенные, — и тогда оно верно и просто не про плагин. (б) Поле стало бы признаком того, что
сессия читает устаревшую копию, — но это **опровергнуто** тем же замером S10-05: правило 9, добавленное
2026-08-05, сессия отдала посимвольно. Прочтение (а) **не замерено**, прочтение (б) исключено.
Что сломается: цитата дословного вывода без указания, что считает поле, читается как утверждение о каталоге.
Это семья F-146 (команда мерит не тот предмет), только предмет здесь чужой — платформенный.
Решение отложено до: человек. Развилки: (а) рядом с цитатой ставится оговорка «поле считает скиллы сессии,
не каталога плагина; сколько именно и чего — `НЕ ПРОВЕРЕНО`»; (б) поле из цитаты убирается, раз к предмету
раздела не относится; (в) ничего.

## F-243 · «Различных номеров на два меньше» — ошибка на единицу, и пришла она из моего файла задачи
Найдено: warden S10-02; главная сессия перемерила и исправила
Класс: ИЗМЕРЕНО.

```bash
rtk proxy grep -c '^## F-[0-9]' plan/FINDINGS.md                              # заголовков 241
rtk proxy grep -o '^## F-[0-9]*' … | rtk proxy grep -o '[0-9]*$' | sort -u | wc -l   # различных 240
rtk proxy grep -o '^## F-[0-9]*' … | rtk proxy grep -o '[0-9]*$' | sort -n | tail -1 # максимум 241
# заголовки − различные = 1; максимум − различные = 1. Двойки нет ни при каком базисе
```

Обе названные причины верны по отдельности — `F-53` действительно возглавляет два блока, `F-49`
действительно не существовал, — но работают против **разных** базисов и не складываются: дубль разводит
заголовки и номера, пропуск разводит максимум и номера, а в счёт заголовков пропуск вклада не даёт вовсе.

Формулировка дословно из Deliverables моего файла задачи; исполнитель перенёс её как велено. То есть
**девятый дефект моих файлов задач за проход**, и ровно тот класс, про который эта же правка и написана —
«число в проверке чаще всего врёт именно у автора файла задачи».
Исправлено главной сессией одной фразой в `ORIENT` §2 (коммит с этой записью рядом), потому что правка —
проза в дев-записи, а не решение.
Решение отложено до: человек — той же развилкой, что F-208.

## F-245 · Цитата F-181 указывает двумя диапазонами в один тест, а число соседей в ней вдвое меньше настоящего
Найдено: warden S10-15
Класс: ИЗМЕРЕНО. Тело F-181 цитирует `test_create_booking.py:74–88` и `:95–109` как **два** теста. В версии
файла на момент находки (`5d6a01d`) `test_two_adjacent_intervals_of_one_room_are_both_created` занимает
строки 79–104, и **оба** диапазона попадают внутрь него. Структурное описание («POST, POST, три `assert`,
два GET») совпадает ровно с ним и ни с чем другим.

Заодно не держится и перепись рядом: «соседних тринадцать тестов» — в файле **28** тестов и 28 блоков
`async with _client`, и `test_two_touching_intervals_of_one_room_are_both_created` несёт то же перемежение
Act/Assert и остался нетронутым.

Исполнитель починил тот тест, который есть, — и только его; Deliverables при этом требовали «правка двух
тестов», то есть моя задача повторила ошибку цитаты.
Что сломается: не поведение — доверие к цитате. Находка сама не говорит, был ли второй тест должен, и это
решает человек.
Решение отложено до: человек. Развилки: (а) второй тест (`…touching…`) приводится к AAA ближайшим
housekeeping'ом, и цитата F-181 исправляется новой записью, а не правкой тела; (б) ничего — один тест
починен, класс назван.

## F-246 · Новый классовый тест даст ложное красное на роуте с `include_in_schema=False`
Найдено: warden S10-15
Класс: ПРЕДСТАВИМО. Такой роут попадёт в `declared` (у него есть `responses=`), но не попадёт в `published`,
и `unpublished` окажется непустым. Сегодня такого роута в пробе нет.
Решение отложено до: человек. Механизма не требует — наблюдение на случай, если роут появится.

- **F-244 → задача S10-20 в эту же волну.** Оба шаблона `test-discovery-invariants` переходят на обход,
  не зависящий от того, развернул ли FastAPI роутеры, и несут страховку от вырождения в тишину. Довод за
  «сейчас», а не «после волны»: каталог этой волной ещё открыт, рабочая форма **уже замерена** в пробе
  задачей S10-15, и дефект — не стиля, а поставки: шаблон отдаёт зелёное, ничего не сравнив, и это первый
  экземпляр класса F-153/F-178 в самом продукте.

## F-249 · `iter_route_contexts` появился в FastAPI 0.137.2, и на более старом пине шаблон упадёт на импорте
Найдено: builder S10-20
Класс: ПРЕДСТАВИМО. Имя введено release-notes FastAPI как замена обхода `router.routes`, версия 0.137.2;
живёт в `fastapi/routing.py` и на верхний уровень `fastapi` не экспортируется. `conventions` §D версий не
пинит, `uv lock` берёт последнюю, так что для нового проекта это невероятно — но проект, унаследовавший
старый `uv.lock`, получит `ImportError` при сборе.
Отказ при этом **громкий**, а не тихий, то есть строго лучше нынешнего зелёного молчания, и лечения не
просит.
Решение отложено до: человек. Записано, чтобы это не выяснялось у первого внешнего потребителя.

## F-251 · Сеть проверяет `real_app`, а параметризация строит своё приложение
Найдено: warden S10-20
Класс: ПРЕДСТАВИМО. `pytest_generate_tests` auth-шаблона строит свой `create_app(container=Container())`, а
новая страховка берёт фикстуру `real_app`. Замеренную причину (обход находит ноль) это покрывает — обход один
и тот же, — но граф роутов, зависящий от контейнера, теоретически разведёт две стороны, и тогда страховка
будет зелёной над скипнутым файлом.
Решение отложено до: человек. Механизма не просит.

## F-252 · Исполнение F-247 сделало ложным довод соседнего шипящегося скилла — четвёртый экземпляр класса за проход
Найдено: builder S10-20, второй круг
Класс: ИЗМЕРЕНО.

`plugins/adw/skills/restapi-route-contracts/SKILL.md:157–164` обосновывает правило «объявляй `422` на каждом
роуте с валидируемым входом» так, дословно: «the `test_openapi_advertises_error_codes` invariant … requires
the decorator to match the spec **exactly**, so a route that omits `422` while carrying a param fails with an
*extra* `422` in the spec».

Замер: тот же шаблон против приложения `adw-rooms`, чьи роуты `422` не объявляют, — **до** правки `FAILED`
с `extra=[422]`, **после** `PASSED`.

Правило само по себе остаётся верным: опубликованный документ честнее, когда декоратор объявляет то, что
FastAPI и так вставит. Но его **единственное записанное основание больше не воспроизводится**, и реализатор,
проверивший довод, снимет `422` с декораторов — а таблица «Operation → error_responses» того же скилла
разъедется с практикой.

**Четвёртый экземпляр одного класса за этот проход** (F-198, F-205, F-250, эта), и у всех один вид: правится
форма, а проза вокруг неё остаётся описывать прежнюю. Ни один не нашёл автор правки — все четыре нашли
исполнители и warden'ы, читая **соседний** текст, а не тот, что правился.
Решение отложено до: человек. Развилки: (а) довод в `restapi-route-contracts` переписывается на тот, который
остаётся верным (документ честнее, а не «инвариант покраснеет»); (б) отдельной задачей; (в) ничего — правило
верно, и основание читают реже, чем правило.

## F-253 · Rule 4 описывает сравнение как точное, а у его шаблона теперь ровно одно изъятие
Найдено: builder S10-20, второй круг
Класс: ИЗМЕРЕНО. Rule 4 скилла говорит «compares decorator-declared codes to spec codes… catches FastAPI bugs
and decorator mismatches alike», без упоминания `_FRAMEWORK_VALIDATION_CODE`, который стоит в шаблоне двумя
вхождениями.
Что сломается: реализатор, следующий Rules, а не копирующий шаблон, воспроизведёт ложное красное F-247
целиком — **тот самый механизм, из-за которого F-250 и понадобилась**. То есть F-250 закрыла обход и не
закрыла изъятие: её собственное исполнение оказалось на одну строку короче.
Решения на это не давали: F-247 назвало шаблон, F-250 назвало «привести Rules 2/4 к новому **обходу**», а
`422` — не обход. Исполнитель верно не тронул.
Решение отложено до: человек.

## F-254 · Посылка решения по F-248 шире своего замера: `I001` в двух шаблонах из шести, а не во всех
Найдено: builder S10-20, второй круг
Класс: ИЗМЕРЕНО. `uv run ruff check` конфигурацией пробы по всем шести извлечённым шаблонам: до правки —
`F401` в openapi плюс `I001` в openapi, auth и info, **`Found 4 errors`**; после — `I001` в auth и info,
**`Found 2 errors`**. `test_cors.py`, `test_request_size_limit.py`, `test_app_constructs.py` чисты.
`ruff --diff` называет единственную причину: одна пустая строка после блока импортов вместо двух.

Диспозиция F-248 отказалась от сквозной правки словами «`I001` — следствие компактного стиля **всех шести**
шаблонов и требует сквозной правки». Замер: двух из шести, и снятие `F401` унесло третий. То есть отказ был
верным по существу (правка не назначена), но **его основание завышено**, и следующий читатель F-248 будет
ждать шести там, где их два.
**Происхождение моё:** формулировку в диспозицию писал я, со слов первого круга, не перемерив.
Решение отложено до: человек. Развилки: (а) остаток честнее описать как «два `I001`, по одной пустой строке
каждый», и тогда решение «ничего» стоит пересмотреть — цена оказалась вдвое меньше названной; (б) ничего —
отказ был по существу верен.

## F-255 · Rule 2 велит брать обе стороны из одного обхода, а сторона документа приходит из `app.openapi()`
Найдено: warden S10-20, второй круг
Класс: ПРЕДСТАВИМО. `test-discovery-invariants/SKILL.md:350` — «Take both sides from the same
`iter_route_contexts(app.routes)` walk as Rule 2», но пояснение за двоеточием описывает только декораторную
сторону; сторона документа в шаблоне приходит из `_declared_codes(app)` → `app.openapi()`, а обход даёт лишь
**ключ** (эффективный путь), по которому две стороны сходятся.
Что сломается: реализатор, следующий Rules вместо копирования шаблона, будет искать коды документа на
`RouteContext`, где их нет. Правка — одно слово, но это правка, а решение 2026-08-06 назначило «привести
Rules к новому **обходу**», и обход назван верно.
Родня F-253: оба — про то, что Rules описывают шаблон приблизительно, и оба всплыли от того, что шаблон
впервые прогнали как код.
Решение отложено до: человек.

## F-259 · Шесть новых механизмов прохода S10 не несут ни даты ревизии, ни условия снятия
Найдено: проверка главной сессии, разбор прохода S10
Класс: ИЗМЕРЕНО. Проход добавил шесть новых обязанностей: мутация с откатом в `evaluator`, субстрат на
диспатче скелета в `implementer`, `BLOCKED` вместо укладки субстрата в `test-author`, кросс-сквозной ярус
тестов там же, форма замеренного разрыва в `accept`, `uv run alembic check` в `infra-persistence`.
Условий снятия в шипящемся дереве ровно два, и ни одно из них не на этих шести:
`rtk proxy grep -rn 'withdrawal condition' plugins/adw/` → `python-style/SKILL.md:293`,
`application/SKILL.md:336`. Оба — прозаические правила на один экземпляр, и оба получили условие снятия
**в том же диффе**.
Что сломается: красная линия 5 требует даты от каждого компонента харнеса ровно затем, чтобы компенсация
исчезнувшего ограничения не носилась бессрочно. Стандарт в проходе был и применён к дешёвым правилам, а к
дорогим — нет. Асимметрия видна и внутри одного класса: `application/SKILL.md:336` получил условие снятия
на один экземпляр, а `domain-model/SKILL.md:36–46` (общий предикат сущности и read-model) — тоже один
экземпляр и без условия.
Решение отложено до: человек.

## F-260 · `alembic check` — обязательная проверка вне `make check`, и в том же диффе сказано, что цель не растит пятую команду
Найдено: проверка главной сессии, разбор прохода S10
Класс: ИЗМЕРЕНО. `plugins/adw/skills/infra-persistence/SKILL.md:460` — «**A change that touched both the
`Table` and a revision runs `uv run alembic check` itself**… Run it by hand because nothing else reports
the drift: lint, type-check and the test suite all stay green through it», и под этим стоит число: на
сюите в 73 теста расхождение `'[)'` против `'[]'` между `Table` и миграцией дало **73 passed**.
`plugins/adw/agents/implementer.md:163` в том же диффе — «That target does not grow a fifth command».
Что сломается: формального противоречия нет — правило демотировано в совет скилла, что красная линия 2
разрешает прямым текстом. Но это единственная в дереве **обязательная** проверка, которую не держит ни
`make check`, ни роль, ни шаг команды цикла: её исполнение зависит от того, вспомнит ли реализатор. Замер
под ней ровно про то, что этот дрейф не ловится ничем другим, — то есть цена невыполнения названа, а
места, где выполнение видно, нет.
Решение отложено до: человек. Развилки: (а) оставить советом и записать это решением, чтобы следующий
проход не открывал вопрос заново; (б) внести в `make check` целевого проекта — тогда бюджет enforcement
перестаёт быть нулём осознанно, а не по недосмотру; (в) отдать роли — назвать в `evaluator` как одну из
проверок зелёной фазы, там где проверка уже наблюдаема в вердикте.

## F-261 · `test-discovery-invariants` требует API FastAPI, которого нет в старых версиях, а субстрат версий не несёт
Найдено: проверка главной сессии, разбор прохода S10
Класс: ПРЕДСТАВИМО. Шаблоны скилла (`SKILL.md:59`, `:167`) импортируют
`from fastapi.routing import APIRoute, RouteContext, iter_route_contexts`. Что на 0.140.13 это работает —
замерено: `cd ~/Projects/adw-rooms && uv run python -c "import fastapi; print(fastapi.__version__)"` →
`0.140.13`, импорт проходит, `dataclasses.fields(RouteContext)` → `['route', '_route_context']`, а
`dependant` / `responses` / `path` резолвятся делегированием — проверено на приложении с
`include_router(prefix='/api')`, где обход отдал `'/api/foos/{id}'` и обе внутренние структуры. Не
замерено — поведение на версии, где этого API ещё нет; поэтому класс `ПРЕДСТАВИМО`, а не `ИЗМЕРЕНО`.
Что сломается: `ImportError` на **сборе**, то есть весь файл инварианта исчезает из прогона — ровно та
тишина, против которой в том же диффе написана непустая проверка `assert operations`, только этажом
раньше. `conventions/SKILL.md:305` запрещает версии в субстрате и прямо называет `fastapi>=0.115`
болезнью, но `:310–316` держит исключение для «known breaking-version boundary», и этот случай подходит
под него буквально.
Что мешает мерить: версия, в которой `iter_route_contexts` появился, не установлена, а `conventions:314`
запрещает писать пол «по памяти о недавней версии». Замер, который снял бы вопрос: поставить в
одноразовом venv несколько версий FastAPI вниз от 0.140 и найти первую, где импорт падает.
Решение отложено до: человек.

## F-262 · Проход S10 оставил очередь глубже, чем нашёл, и это самая большая партия за всю историю заходов
Найдено: проверка главной сессии, разбор прохода S10
Класс: ИЗМЕРЕНО. Реестр: `git show origin/main:plan/FINDINGS.md | grep -c '^## F-[0-9]'` → `186`,
`grep -c '^## F-[0-9]' plan/FINDINGS.md` → `255`, то есть **+69** за проход. Открытых по форме
`ORIENT.md` §5 — **48**, и все из полосы F-197…F-255, то есть **все открыты этим же проходом**: интервью
закрыло 60 записей и оставило 48 своих. Объём: `git diff --numstat origin/main..HEAD` с разделением по
префиксу `plugins/adw/` → шипящееся `+346 −91`, дев-запись `+4742 −32`.
Против прошлых проходов (границы взяты по первому коммиту, добавляющему `plan/S0N-*.md`: `87cc09b`,
`b6f4283`, `080f52e`, `ce6c927`, `05792b8`, `81311e0`, и `origin/main` для S10; числа воспроизводятся
`git show <граница>^:plan/FINDINGS.md | grep -c '^## F-[0-9]'`):
S04 +43, S05 +14, S06 +6, S07 +9, S08 +43, S09 +16, **S10 +69**. Задач: 12, 6, 3, 2, 8, 6, **20**.
Что сломается: сам по себе рост шипящегося мал — 10 069 → 10 324 строки, +2,5%, ноль новых файлов в
плагине (`git diff --name-status --diff-filter=ADR origin/main..HEAD -- plugins/adw` пуст), и это **не**
профиль третьего захода. Опасен не продукт, а размер партии: проход, взявший все 60 открытых записей
разом, дал хвост в 69 новых, и следующий проход стартует с очередью больше той, с которой начинал этот.
Проходы S05–S07 закрывали по 3–6 задач и давали +6…+14. Если правило размера не записано, следующее
интервью снова возьмёт «все открытые», потому что именно так и выглядит добросовестность.
Решение отложено до: человек. Развилки: (а) записать потолок партии — сколько задач за проход, — и тогда
это правило, а не наблюдение; (б) ничего не менять, но тогда цифра +69 остаётся как база для сравнения со
следующим проходом.

## F-263 · `claude plugin validate` объявляет фронтматтер семи шипящихся файлов непарсящимся, а рантайм его читает
Найдено: проверка главной сессии, подготовка шипа 1.2.0
Класс: ИЗМЕРЕНО. `claude plugin validate plugins/adw` → **7 ошибок**, по одной на файл, все одинаковые:
«YAML frontmatter failed to parse: YAML Parse error: Unexpected token. At runtime this skill loads with
empty metadata (**all frontmatter fields silently dropped**)». Файлы: `agents/implementer.md`,
`agents/test-author.md`, `skills/domain-exception`, `domain-service`, `infra-capability-adapter`,
`patterns`, `restapi-endpoint`.
Причина найдена: во всех семи `description` или `when_to_use` несёт `: ` (двоеточие с пробелом) внутри
незакавыченного скаляра — `«…a domain capability protocol: object storage…»`, `«They nest: compensation
outside…»`, `«…thin: parse input…»`. Строгий YAML на этом ломает **весь блок**, а не одно поле.
**Утверждение валидатора о рантайме неверно, и это замерено двумя способами.** Первый:
`claude plugin details adw@wntic-adw` показывает у всех семи ненулевую always-on стоимость —
`patterns ~170`, `implementer ~230`, `domain-service ~140`, `domain-exception ~120`,
`infra-capability-adapter ~150`, `restapi-endpoint ~120`, `test-author ~140`, — а always-on скилла это и
есть его фронтматтер; у отброшенного он был бы нулевым, и для сравнения `python-style` и `architecture`,
парсящиеся чисто, стоят те же `~170`. Второй: `patterns` стоит в списке скиллов этой самой сессии со
своими `description` и `when_to_use` целиком. То есть парсер рантайма мягче валидаторского и режет строку
по первому двоеточию, а не гоняет YAML.
Не регрессия волны S10: тот же прогон на `eb0c5c0` (дерево до волны) даёт те же **7** ошибок и тот же
список файлов. Дефект старше публикации 1.1.0.
Что сломается: сегодня — ничего, и поэтому механизма это не просит. Но цена нулевая, а риск
несимметричный: если рантайм когда-нибудь перейдёт на строгий YAML — а валидатор уже на нём, — семь
файлов потеряют метаданные **молча**, ровно тем способом, против которого написан весь `PLATFORM.md`.
И `validate --strict` нельзя поставить в проверку шипа, пока эти семь красные: единственная проверка
манифеста, которую даёт платформа, сейчас нечитаема.
Решение отложено до: человек. Развилки: (а) закавычить семь значений и включить `claude plugin validate
--strict` в подготовку шипа; (б) убрать `: ` из семи формулировок — дешевле кавычек и не трогает YAML
вовсе; (в) ничего, записав, что расхождение известно и рантайм замерен.

## F-264 · Явное обновление плагина пишет семвер в запись установки — прошлый вывод был верен только для пуша
Найдено: проверка главной сессии, шип 1.2.0
Класс: ИЗМЕРЕНО. Три снимка `~/.claude/plugins/installed_plugins.json` за один сеанс.
**До и после `git push origin main`** (`eb0c5c0..ec6a11b`, 64 коммита) — файл побайтово одинаков, `diff`
пуст. Это воспроизводит замер 2026-08-05 и подтверждает: публикация записи установки не касается.
**После `claude plugin update adw@wntic-adw --scope project`** из `~/Projects/adw-probe` запись
изменилась целиком: `version` `89ac6e8c8ea9` → **`1.2.0`**, `installPath`
`cache/wntic-adw/adw/89ac6e8c8ea9` → `cache/wntic-adw/adw/1.2.0`, `gitCommitSha` → `ec6a11b…`,
`lastUpdated` → момент прогона. Вывод CLI: «Plugin "adw" updated from 89ac6e8c8ea9 to 1.2.0 for scope
project. **Restart to apply changes.**»
Отсюда поправка к записи 2026-08-05. Оборот «семвер в запись установки не попадает вовсе» был выведен из
двух наблюдений — пуш ничего не меняет, и у первой пробы в поле `version` стоял SHA, — и первое верно, а
второе объяснялось не форматом источника, а **временем установки**: пробу ставили 2026-07-30, когда в
`plugin.json` ключа `version` ещё не было, поэтому в поле лёг SHA. Как только ключ появился, семвер туда
пишется. Третий экземпляр подтверждает это независимо: запись `scope: local` для `~/Projects/crawler`,
поставленная 2026-08-07, несёт `version: "1.1.0"` — тот же directory-источник, тот же кэш-путь, семвер на
месте.
Правило, которое из этого следует: **версию читает установка, а не публикация.** Директорийный источник
семверу не мешает.
Побочное наблюдение без решения: кэш накапливает каталог на каждую версию и на каждый SHA —
`ls ~/.claude/plugins/cache/wntic-adw/adw/` даёт 10 каталогов, из них 8 по SHA. Никто их не подчищает.
Решение отложено до: человек — в частности, переезжает ли этот замер в `plan/PLATFORM.md`, где ему место
по предмету.

## F-265 · Кэш и версия плагина на этой машине инертны: все потребители читают рабочее дерево, и ни один прогон не приколочен
Найдено: проверка главной сессии, попытка «поставить плагин как в adw-rooms везде»
Класс: ИЗМЕРЕНО. Вопрос был, какая из двух проводок лучше — `adw-rooms` (directory-источник, записи об
установке нет) или `adw-probe` (та же проводка плюс кэш-установка). Замер показал, что различия нет:
читают дерево обе, и `crawler` тоже.
Причина в `~/.claude/plugins/known_marketplaces.json`: маркетплейс `wntic-adw` зарегистрирован
**глобально**, `"source": {"source": "directory", "path": ".../agentic-development-workflow"}`, а
`installLocation` — **само рабочее дерево**, не копия. Через него ходит каждый проект, включивший
`adw@wntic-adw`.
Эксперимент: в дерево положен скилл `zz-freshness-probe` с сентинелом, затем каждому потребителю задан
вопрос отдельной сессией `claude -p`, есть ли у него такой скилл; затем скилл удалён и вопрос повторён.

| потребитель | запись установки | сентинел в его кэше | сессия при сентинеле в дереве | после удаления |
|---|---|---|---|---|
| `adw-rooms` | нет | — | **YES** | NO |
| `adw-probe` | `1.2.0`, кэш `adw/1.2.0` | нет | **YES** | NO |
| `crawler`   | `1.1.0`, кэш `adw/1.1.0` | нет | **YES** | NO |

Отрицательный контроль отработал у всех трёх, то есть «YES» не был артефактом вопроса. Ни в одном кэше
сентинела не было — значит сессия читала не кэш.
Побочно: `claude plugin details` для этого вопроса **не инструмент** — он показал 35 скиллов даже там, где
кэш нёс 34, то есть тоже разрешает через источник, а не через установку.
Следствия. Первое: ставить «как в adw-rooms» некуда — так уже везде, и проводка `adw-rooms` не особая,
она просто без рудимента. Второе: запись установки и каталог кэша хранят версию, **которую никто не
читает**; обновление `adw-probe` до `1.2.0`, сделанное часом раньше, поменяло только бухгалтерию, а
«Restart to apply changes» относилось к копии, которую сессия не открывает. Это уточняет F-264: версию
читает установка, но саму установку в этой конфигурации не читает никто. Третье: подъём 1.1.0 → 1.2.0 на
этой машине не доставляет ничего — он маркер для человека, читающего репозиторий, и только.
Что сломается — и это главное, а не уборка кэша: **ни один прогон пробы не приколочен к версии плагина.**
Правка воркфлоу во время прогона меняет плагин под ним. Весь дефект-лог этого репозитория атрибутирует
находки прогонам («измерено на change 003»), и эта атрибуция молча предполагает неподвижный плагин.
Предположение неверно, и случай не гипотетический: волна S10 за 14 часов правила десять шипящихся скиллов,
обе пробы всё это время смотрели в дерево.
Решение отложено до: человек. Развилки: (а) ничего — принять, что пробы всегда на острие, и записать это;
(б) писать SHA дерева воркфлоу в вердикт прогона, чтобы атрибуция была проверяемой задним числом; (в)
разводить роли — пробу для острия оставить на directory-источнике, а вторую посадить на приколоченную
копию, и тогда кэш перестаёт быть мусором и становится механизмом. Уборка двух оставшихся каталогов кэша
(`1.1.0`, `1.2.0`, 1.3M) — вопрос отдельный и косметический: они инертны, но их сносят вместе с записями
установки, а не `rm`.

## F-266 · Таймлайн тулчейна в отчёте слеп к прогонам сьюта из скрипта: весь адверсарный проход дал в нём ноль строк
Найдено: разбор прогона 003 второй пробы, проверка главной сессии
Класс: ИЗМЕРЕНО. Вердикт (`git show 36c0ca3:specs/changes/003-list-room-bookings/verdict.md`) называет
**двадцать** неверных имплементаций, «built and run», каждую с полным прогоном сьюта и своим числом
падений. В логе оценщика их вывод — результат **одного** вызова `Bash`:
`rtk proxy grep -c '"command": "uv run python .*mutate\.py' log-adw:evaluator-a3069bf542e4.md` → `1`, и
`rtk proxy grep -o '^### AC-[0-9ab]*' …` → 18 различных блоков, каждый со строкой `suite: N failed …`
(двух из двадцати в логе не видно — вывод усечён при рендере). В таблице «Toolchain timeline»
`SUMMARY.md` у оценщика **7** строк, и ни одна не про этот вызов: `rtk proxy grep -n 'mutate' SUMMARY.md`
даёт ровно одну строку, `:106`, и это таблица churn, а не таймлайн.
Что сломается: строка «adw:evaluator — 7 run(s), 2 red» читается как полный счёт его прогонов, и
разбор 003 её так и прочёл до диффа — «два красных, это его мутации». На деле мутаций двадцать, красных
среди них двадцать, и две видимые не соотносятся с ними ни числом, ни смыслом. Класс шире оценщика:
любая роль, загнавшая цикл прогонов в скрипт, исчезает из единственной таблицы, по которой §5 ORIENT
велит судить, «сработал ли цикл». Ошибка тихая и правдоподобная — тот же класс, что F-85 и F-194.
Решение отложено до: человек. Развилки: (а) ничего — принять, что таймлайн считает вызовы, а не
прогоны, и это его определение; (б) третья оговорка в §5 ORIENT рядом с двумя существующими
(`<synthetic>` в ростере, ожидание в `active`); (в) пятым пунктом в посылку S10-17 для
`claude-agent-report` — там уже едут четыре.

## F-267 · Сдача прогона человеку поместила 36-часовой простой между `/adw:build` и `/adw:accept`, а он весь лежит после `/adw:accept`
Найдено: разбор прогона 003 второй пробы, проверка главной сессии
Класс: ИЗМЕРЕНО. Сдача прогона говорит: «Span reads 2311m — that is wall clock across two days
including the gap between your /adw:build and /adw:accept». Границы сегментов главной сессии
(`rtk proxy grep -n '^## ▶ Segment' log-main-session-cdf7ba2b-42a.md`, длительности — из §2 `SUMMARY.md`):
сегмент 5 (последняя нотификация `/adw:build`) стартует `05:39:03.689Z` и идёт 20м55с, то есть кончается
≈`05:59:58Z`; сегмент 6 (`/adw:accept`) стартует `06:00:19.055Z`. Между сборкой и приёмкой —
**21 секунда**. Сегмент 6 идёт 24м26с и кончается ≈`06:24:45Z`; сегмент 7 (`agent-report`) стартует
`2026-08-07T19:04:06.294Z`. Весь простой — 36ч39м — лежит между приёмкой и вызовом отчёта.
Что сломается: сдача — то немногое, что человек читает целиком, и утверждение в ней про его собственный
рабочий ритм он проверять не станет. Здесь оно приписывает паузу тому месту цикла, где паузы не было
вовсе. Семейство ORIENT §4 «писать по памяти о файле, который не открыл», с поворотом: таблица была
открыта, из неё взята одна цифра (`span`), а вторая (границы сегментов) — не взята, хотя лежала
строкой ниже.
Решение отложено до: человек. Развилки: (а) «не наступило» — экземпляр один, сигнал к переоткрытию —
второй; (б) строка в §5 ORIENT о том, что `span` минус `active` называет величину простоя и **не**
называет его место; (в) в посылку для `claude-agent-report`: отчёт мог бы сам печатать разрыв между
сегментами, и тогда угадывать было бы нечего.

## F-268 · Критерий, чью фикстуру запрещает инвариант той же способности: два экземпляра в одном изменении, и второй пойман уже внутри цикла
Найдено: разбор прогона 003 второй пробы, проверка главной сессии
Класс: ИЗМЕРЕНО. Первый экземпляр интервью поймало само: сообщение коммита спеки `1afa3db` разбирает
тай-брейк двух броней с одним стартом и заключает, что «within one room it cannot happen» — критерий
переписан **до** коммита, ценой ноль. Второй прошёл интервью насквозь: `e3deb1a`, правка спеки уже
в ходе `/adw:build`, — AC-2 требовал в одном ответе трёх геометрий, из которых охватывающая
перекрывает обе крайние, а комната не держит двух перекрывающихся, поэтому «the store refuses the seed
and the call can never exist». Сообщение того же коммита само называет повтор: «This is the same
drafting slip as the AC-4 tie-break». Цена второго — вопрос автора тестов вместо теста, `AskUserQuestion`
человеку внутри сборки (лог главной сессии, сегмент 3, строки `:2101` и `:2332`) и отдельный коммит
правки спеки в середине цикла.
Что сломается: черновой критерий проверяется против живой спеки способности **не системно** — один раз
проверили, другой нет, и различие между ними ничем не объяснено, кроме внимания. Промах дешёвый, пока
его ловит автор тестов до baseline: после baseline формулировка критерия заморожена, и тот же промах
стоил бы возврата через `CONTRACT-CHANGE`. Оба экземпляра — против инварианта, который положило
**предыдущее** изменение той же способности (правило столкновений из 002), то есть класс растёт ровно
с тем, ради чего живая спека компаундится.
Решение отложено до: человек. Развилки: (а) ничего — оба поймались, цена одного круга; (б) строка в
`/adw:spec`: каждый черновой критерий, называющий больше одной сущности в одном состоянии, читается
против инвариантов живой спеки прежде, чем попасть в `criteria.md`; (в) отдать это фазе ревью красного,
где второй экземпляр и всплыл, и не трогать интервью.

## F-269 · `list` и `count` идут двумя сессиями, и ни один критерий не может увидеть их расхождение
Найдено: разбор прогона 003 второй пробы, чтение диффа
Класс: ИЗМЕРЕНО — форма кода и предписание скилла; **провала не наблюдалось**, и по красной линии 1
запись механизма не просит. `git show change/003:src/rooms/application/bookings/list_bookings_handler.py`:
`items=await self._repo.list(...)`, затем `total=await self._repo.count(...)` — два `await` подряд.
`git show change/003:src/rooms/infrastructure/postgres/repositories/booking_repository.py`: каждый из
двух методов открывает свою `async with self._sf() as session`, то есть свою транзакцию. Дом этой формы
— каталог: `infra-persistence` даёт `count(*, filter)` отдельным методом (правило 10, `:391`, шаблон
`:290`), `application` — `total = await self._repo.count(...)` в теле хендлера (`:194`). Про то, что
две цифры должны прийти из одного снимка, не говорит ни один из двух.
Что сломается: под конкурентной записью между двумя вызовами страница и `total` описывают разные
состояния таблицы — `total`, меньший длины `items`, или пагинация, у которой последняя страница пуста
при ненулевом `total`. Критерии AC-7 и AC-8 закрывают арифметику страницы на **неподвижных** данных и
эту разновидность не видят по устройству; интеграционный ярус тоже — он последователен. Замечено
чтением диффа, а не прогоном: экземпляров ноль, и в пробе, где пишет один тест, их и не будет.
Решение отложено до: человек. Развилки: (а) ничего — записать, что house form отдаёт две цифры двумя
транзакциями сознательно, и это цена простоты; (б) строка в `infra-persistence` о том, что `list` и
`count` одного ответа берутся одной сессией, когда ответ обещает их согласованность; (в) вопрос
предъявляется живой спеке целевого проекта как ослабляемый инвариант — по F-112, туда же, где уже
лежит разрыв AC-4.

## F-270 · Baseline закоммичен с двумя критериями, чьи тесты умели только скипаться, — и когда они впервые исполнились, оказалось, что доказать они не могли ничего
Найдено: разбор прогона 001 в `crawler`, проверка главной сессии
Класс: ИЗМЕРЕНО. Baseline `c6f0f79` прошёл **четыре** круга ревью красной фазы (три `FAIL`, четвёртый
`PASS` — `rtk proxy grep -o 'VERDICT: [A-Z]*' log-adw:test-review-*.md`), и в момент коммита AC-1 и AC-2
не исполнялись ни разу: их тесты скипались за отсутствием учётных данных Telegram. Скип был **санкционирован
спекой** — секция `Verification` требовала «skip loudly», и диспетчерский промпт сообщал это ревьюеру
дословно (`log-adw:test-review-a2c2c9e1401e.md:34`), — то есть ревьюер не проглядел, а был извещён.
Когда учётные данные появились, первый же настоящий прогон упал, и причина замерена, а не угадана:
Telethon-клиент никогда не получает **собственное** исходящее сообщение как событие `NewMessage`, а оба
теста постили тем же клиентом, на котором слушает воркер. То есть они не прошли бы **ни против какой**
имплементации. Автор тестов назвал это предположение сам, ещё в красной фазе, и таскал его нерешённым
через три своих сегмента (`log-adw:test-author-a22b442f3c8e.md:3080`, `:3825`, `:4710`), а сообщение
коммита правки говорит прямо: «The test author had flagged this exact assumption in the red phase and it
was not acted on».
Что сломается: гарантия красной фазы — «тест падает по правильной причине» — для скипающегося критерия
не действует вовсе, и ничто этого не помечает: в вердикте он выглядит как остальные. Здесь это стоило
двух правок спеки после baseline (`79aabde`, `04b0f27`), первых в истории заходов правок `tests/**`
после baseline (`8e70173`, 127 строк в двух файлах) и третьего диспатча оценщика. Дёшево обошлось
только потому, что дефект был в механизме теста; будь он в критерии, формулировка была бы уже заморожена.
Решение отложено до: человек. Развилки: (а) ничего — записать, что живой критерий проверяется только
там, где среда есть, и это цена живых доказательств; (б) `test-review` обязан отдельной строкой назвать
каждый помеченный тест, который в его прогоне **не исполнился**, и красная фаза без исполнения не
считается пройденной для этого критерия; (в) вопрос роли, оставшийся без ответа, не даёт закрыть фазу —
адресат тогда не ревью, а шаг 3 `build.md`.

## F-271 · У `VERDICT: FAIL`, причина которого не в имплементации, в `build.md` нет маршрута
Найдено: разбор прогона 001 в `crawler`, проверка главной сессии
Класс: ИЗМЕРЕНО. `build.md:261` даёт одну ветку: «**`VERDICT: FAIL`** → a **new** dispatch of step 5»,
то есть новый диспатч имплементатора, и он же тратит единицу потолка (`:264`). Вердикт `98405fb` был
`FAIL` по причине, которой имплементатор не лечит: «their marked tests skip for want of Telegram
test-DC credentials». Прогон поехал не по маршруту и правильно сделал: две правки спеки решением
человека, минтинг сессий Telegram руками человека через `<bash-input>` (сегменты 11–24 главной сессии,
пять переписываний `<scratch>/mint.py`), правка тестов и **третий** диспатч оценщика. Ни одного диспатча
имплементатора при этом не потрачено, то есть счёт потолка остался верен случайно, а не по правилу.
Что сломается: единственная ветка после `FAIL` предполагает, что виновата имплементация. Оркестратор,
читающий команду буквально, отправляет имплементатора чинить окружение — и тратит на это половину
потолка, после чего у настоящей починки остаётся одна попытка. Здесь буквального чтения не случилось,
но и опоры в командном файле у отклонения не было: маршрут придуман на месте.
Решение отложено до: человек. Развилки: (а) ничего — экземпляр один, оркестратор справился;
(б) в §7 `build.md` вторая ветка: `FAIL`, чью причину не устраняет правка `src/`, идёт к человеку и
потолка не тратит — по образцу трёх уже описанных там диспатчей, которые «не потратили ничего»;
(в) запретить принимать `FAIL` по недоказанному критерию как окончание цикла вообще.

## F-272 · Оба диспатча имплементатора уложили 64 файла greenfield, не вызвав ни одного скилла
Найдено: разбор прогона 001 в `crawler`, проверка главной сессии
Класс: ИЗМЕРЕНО. `for f in log-*.md; do rtk proxy grep -c 'tool_use\*\* `Skill`' "$f"; done` по всем
одиннадцати файлам отчёта: **2**, и оба — у автора тестов (`adw:test-discovery-invariants`,
`adw:test-architecture-rule`). У обоих имплементаторов **ноль** при 54 и 10 созданных файлах. Днём
раньше, в `adw-rooms` 003, та же роль на изменении вдесятеро меньшем вызвала **десять** скиллов
(шесть на скелете, четыре на коде). У `test-review` и `evaluator` ноль ожидаем: им `test-principles`
предзагружен полем `skills:`.
Два наблюдения не дают свалить это на платформу. Первое: `Skill` в этой сессии работал — автор тестов
вызвал два. Второе: три скилла имплементатору предзагружены (`conventions`, `architecture`,
`python-style`), то есть без единого вызова он всё же держал ядро house style — но `domain-model`,
`application`, `infra-persistence`, `domain-ports` в его контекст не попали ни одним из двух способов.
Отличие сессии, которое стоит проверить прежде любых выводов: плагин здесь **поставлен внутрь сессии**,
а не до её старта, — а что происходит со скиллами каталога, созданного после старта, в `PLATFORM.md`
помечено НЕ ПРОВЕРЕНО (F-60).
Что сломается: каталог из 30 скиллов — это и есть ядро воркфлоу по §1; роль, не дотянувшаяся до него на
самом большом изменении, какое бывает (greenfield), строит по трём предзагруженным и по памяти модели.
Проверить, разошёлся ли код с каталогом, разбор **не пробовал** — это отдельная работа и она не сделана.
Решение отложено до: человек. Развилки: (а) замер prober'ом: тот же диспатч в сессии, стартовавшей с
уже установленным плагином, против сессии, ставящей его внутрь, — и это же закрывает пункт F-60;
(б) сравнить код `crawler` с каталогом и посмотреть, стоила ли непризванность чего-нибудь;
(в) расширить `skills:` имплементатора — но это ровно то, что реструктуризация сузила с девяти тел до
трёх, и без замера (а) поворот назад не обоснован.

## F-276 · Warden по своему определению сам гоняет `git status --short` — в параллельной волне он видит файлы чужих задач
Найдено: разбор трёх репо-агентов главной сессией, S11
Класс: ПРЕДСТАВИМО. Соседний замер — F-99: у builder'ов `git status` в параллельной волне дал ложный
красный на честной работе, и протокол перевёл проверки на `git diff --stat -- <свои пути>`.
Определение warden'а («What you read first», п. 3) осталось на `git status --short` + `git diff` без
оговорки о параллельном следе. У warden'ов зафиксированного ложного вердикта нет: оба ревью S11 (и
ревью волн S09–S10) получали список файлов и оговорку от главной сессии в промпте диспатча — правило
держится дисциплиной диспатчера, не определением роли.
Что сломается: warden, диспатчнутый без оговорки при живом чужом дифе, прочитает незакоммиченный файл
соседней задачи как «work outside Deliverables» — ложный ОТКЛОНИТЬ на честной работе. Сигнал к
переоткрытию как ИЗМЕРЕНО: первый такой вердикт.
Решение отложено до: человек.

## F-277 · Проверка S11-00 предъявила путь шире своего: `.claude/` вместо `.claude/commands`, и параллельная задача законно пишет внутрь
Найдено: проверка главной сессии, она же автор файла задачи — промах свой
Класс: ИЗМЕРЕНО. Строка проверки в `plan/S11-00-command-symlink-probe.md`:
`git status --porcelain -- .claude/` с ожиданием «после: пусто». В той же волне S11-01 создаёт
`.claude/agents/adw-analyst.md` — прогнанная после возврата builder'а, команда возвращает непустое на
честной работе. Не выстрелило только порядком: проверка прогналась до возврата S11-01. Класс — тот же,
что замеренная F-172 (проверка предъявляет дифф не по своим путям), правило после которой уже записано
в `ORIENT` §4 — и не удержало автора, читавшего это правило в тот же день.
Что сломается: ложный красный на честной работе при другом порядке возвратов; исполненный файл задачи
задним числом не правится (ruling F-71), поэтому экземпляр останется в тексте и ловится только этой
записью.
Решение отложено до: человек.

## F-278 · Сигнал churn дважды подтверждён разборами, но не имеет нумерованной записи — и потому не смог войти в watch-list
Найдено: S11-01, builder (подтверждено проверкой главной сессии)
Класс: ИЗМЕРЕНО. `rtk proxy grep -n 'churn' plan/FINDINGS.md` → две строки, обе внутри чужих тел;
ближайший замер — девять переписываний `test_list_bookings.py` как след круга ревью — лежит
ненумерованным в блоке «Замеры, которым запись в реестре не нужна» (`FINDINGS.md:7497–7501`). Решение
человека 2026-08-09 требует номер измеренной находки на каждой строке watch-list — строка про churn
отброшена builder'ом правомерно.
Что сломается: файл, переписанный несколькими агентами, — место, где прогон буксовал (это показывали
разборы 003 rooms и сводки SUMMARY), но analyst этому сигналу не учится, пока человек либо не даст
замеру номер, либо не снимет требование номера для этой строки.
Решение отложено до: человек.

## F-279 · Список тел для watch-list разошёлся со списком «Читать сначала» задачи на одну запись — F-195 понадобилась, но названа не была
Найдено: S11-01, builder
Класс: ИЗМЕРЕНО. Строка watch-list про отказы инструментов не подкрепляется ни одной из восьми записей,
названных в «Читать сначала» `plan/S11-01-analyst-agent.md`; builder нашёл и прочитал тело F-195
(`FINDINGS.md:5570–5589`, `DENIED Bash — kind automode-unavailable`) сам. Промах автора файла задачи:
кандидат-строку про отказы задача дала, а тело под неё — нет.
Что сломается: ничего немедленно — строка в `adw-analyst.md` несёт F-195 и удовлетворяет решению;
но warden, строго сверяющий номера с «Читать сначала», увидел бы номер вне списка, и без отчёта
builder'а это читалось бы как память, а не как сверенная цитата.
Решение отложено до: человек.

## F-281 · Коммит главной сессии между возвратом задачи и её коммитом снёс неотслеживаемый deliverable — pre-commit стэшил чужой дифф
Найдено: S11-02, builder (второй заход, после вердикта warden'а); механизм восстановлен главной сессией
Класс: ИЗМЕРЕНО — само исчезновение: `/bin/ls .claude/commands/` → `No such file or directory` при
живых ` D .claude/commands` и ` M CLAUDE.md` в `git status -uall` — стёрто ровно неотслеживаемое
(четыре симлинка + `debrief.md`), отслеживаемое цело. Механизм — гипотеза, названная как гипотеза:
по времени совпадает коммит F-280 главной сессией при живом незакоммиченном дифе S11-02; pre-commit
печатал «Stashing unstaged files … Restored changes» дважды, а восстановление снятого **симлинка**
`.claude/commands` поверх реального каталога того же имени не может не убрать каталог. Builder
восстановил содержимое побайтно из своего контекста; при холодном контексте deliverable был бы
потерян.
Что сломается: протокол уже запрещает двум писателям один файл, но этот случай в щели между правилами —
файлы не пересекались, пересеклись **имя пути** (симлинк ↔ каталог) и сам факт коммита при чужом
незакоммиченном дифе. Любой коммит главной сессии в этой щели (или чей-то `git clean -fd`) молча
уничтожает неотслеживаемую часть чужой честной работы.
Решение отложено до: человек. Развилки: (а) строка в протокол цепочки: главная сессия не коммитит,
пока в дереве живёт незакоммиченный дифф диспатчнутой задачи; (б) «не наступило дважды» — записать и
ждать второго экземпляра; (в) коммитить deliverable задачи сразу по возврату builder'а, до warden'а,
с правкой по вердикту вторым коммитом — меняет смысл «коммит = принятая задача».

---

## Решения человека, 2026-08-09 — дополнения ко второму решению

Перенесены сюда волной S17 **дословно**, без единой правки текста: они висели без заголовка между
телами записей — артефакт побайтового извлечения реестра, тот же класс, что `s13-wave-1`. Это живые
решения, а не тела находок: на первое опирается счёт красной линии 3, на второе — правило языка
`CLAUDE.md`. Номера F-275 и F-282…F-284, которые они называют, закрыты и вынесены из этого файла —
их тела читаются из git-истории.

**Дополнение решения 2, 2026-08-09 — `agent-report` становится вторым плагином этого маркетплейса, и правило счёта.**
Решение человека, из продолжения того же интервью. Хотелка названа прямо: «хочу избавиться от
agent-report» как от отдельной сущности. Вариант «скрипт внутрь `plugins/adw/`» отклонён (красная
линия 2 — бюджет ноль, слой «ноль» §1 — парсер JSON-пейлоадов не переносится, и обе причины родом из
17 200 строк); выбран перенос: репозиторий `~/Projects/claude-agent-report` (1.1.2, дерево чистое,
`main == origin/main`, `c1d2dee`) переезжает в `plugins/agent-report/` этого маркетплейса, внешний
репозиторий после миграции архивируется человеком. Красные линии правят воркфлоу `adw`;
`plugins/agent-report/` — платформенный инструмент наблюдаемости со своим жизненным циклом, его
скрипт легален и в бюджет enforcement воркфлоу не входит; проверка красной линии 2 и так скоупится
`plugins/adw` и остаётся верной дословно. Следствия: (1) прежняя форма исполнения S10-17 — «четыре
issue в чужой репозиторий» — снята: инструмент теперь наш, записи F-150, F-153, F-157, F-194 остаются
открытыми в реестре и чинятся обычным порядком, интервью → задачи; (2) **счёт красной линии 3**: ни
проход S11, ни перенос S12 итерацией воркфлоу не считаются — дословно: «Текущую сессию не нужно
учитывать в счёте. Мы здесь закрываем не находки, а просто мои хотелки, потому что я хочу упростить
улучшение воркфлоу». Счёт остаётся **10 фич : 7 проходов**; фраза «этот проход — восьмой, счёт станет
10:8» выше и её копия в `INDEX.md` отменены этим решением, в `INDEX.md` — поправкой по образцу F-258.

**Дополнение решения, 2026-08-09, поздним тем же днём — язык.** Решение человека, дословно: «Все
агенты, скиллы, команды должны быть строго на английском языке». Это расширяет правило языка
`CLAUDE.md`: английский не по признаку «шипится или производится», а по **типу файла** — любой файл
агента, скилла или команды, включая project-scoped дев-обвязку (`.claude/agents/`,
`.claude/commands/`). Шов, который остаётся: **цитаты русских идентификаторов дев-записи** (имена
секций файлов задач — «Читать сначала», «Проверка») и **рантайм-вывод, предназначенный русской
дев-записи** (форма вердикта warden'а, скелет `НАХОДКА` / классы `ИЗМЕРЕНО`/`ПРЕДСТАВИМО`) — это
данные, не проза, и они остаются как есть. По этому шву `adw-analyst.md` и три старших репо-агента
уже соответствуют; нарушители — `debrief.md` и `interview.md`, написанные по-русски как дев-запись,
— переводятся немедленно, теми же билдерами. Исполненные файлы задач S11-02/S11-03, велевшие
«по-русски», задним числом не правятся (ruling F-71) — расхождение закрывает эта запись. Строка
`ORCHESTRATE.md` о принятом смешении в `.claude/agents/` перекрыта этим решением в части прозы;
сам файл и так под F-275.
