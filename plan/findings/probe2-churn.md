# Находка разбора прогона rooms-003 — churn как сигнал буксования, 2026-08-10

Источник: churn-таблица отчёта `run-report` прогона 003 `adw-rooms` и разбор того прогона; запись
заведена решением человека 2026-08-10 (F-278) — замер дважды подтверждён разборами, но не имел
нумерованной записи и потому не мог войти в watch-list. Формат и правила — `plan/ORIENT.md` §5.
После исполнения файл умирает закрывающим коммитом волны; номер `probe2-churn-1` остаётся разрешимым
из git-истории, как вся F-серия.

## probe2-churn-1 · Файл, переписанный многими заходами, — след круга, в котором прогон буксовал
Найдено: разбор прогона 003 `adw-rooms`; вторично — сводки SUMMARY той же пробы
Класс: ИЗМЕРЕНО. Churn-таблица отчёта 003: **девять переписываний `test_list_bookings.py`** — и это
след конкретного круга: первый вердикт `test-review` — `FAIL` с тремя требуемыми правками (первая:
AC-16 был закреплён только тестами, зелёными до написания кода), автор закрыл все три, ревьюер
перепроверил вторым проходом против дерева. Замер лежал ненумерованным в блоке «Замеры, которым
запись в реестре не нужна» снятого журнала (`git show <sha>^:plan/FINDINGS.md`, строки 7497–7501).
Что сломается: ничего само по себе — высокий churn одного файла не дефект, а маркер: там, где один
файл переписан многими заходами, прогон буксовал, и разбор, не читающий churn-таблицу, этот круг не
увидит. Watch-list analyst'а требует номер замера на каждой строке (решение 2026-08-09) — эта запись
и есть номер.

## Решения человека, 2026-08-10 — исполнение F-278

- **probe2-churn-1 → строка в watch-list `adw-analyst.md`**, десятая из десяти по потолку решения
  2026-08-09, с этим номером. Больше ничего: маркер, не правило и не порог.
