# plan/ — реализация шага 1, статус и протокол

Шаг 1 из `WORKFLOW.md` §10: **адаптер**. Четыре агента, три команды, шаблоны, манифесты.
Ноль скриптов — это не пожелание, а красная линия 2 (бюджет enforcement = ноль).

`plan/` — **дев-запись, она не шипится.** Правило поставки по местоположению: файл шипится
тогда и только тогда, когда лежит под `plugins/adw/`.

---

## Статус

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| B01 | [платформенная проба](B01-platform-probe.md) | — | — | ☑ |
| B02 | [четыре агента](B02-agents.md) | адаптер | B01 | ☑ |
| B03 | [шаблоны артефактов](B03-templates.md) | ядро | — | ☑ |
| B04 | [`/adw:spec`](B04-spec-command.md) | адаптер | B03 | ☑ |
| B05 | [`/adw:build`](B05-build-command.md) | адаптер | B01, B02 | ☑ |
| B06 | [`/adw:accept`](B06-accept-command.md) | адаптер | B03 | ☑ |
| B07 | [манифесты и репетиция установки](B07-manifests.md) | адаптер | B02, B04, B05, B06 | ☑ |

Порядок: **B01 → (B02 ∥ B03) → (B04 ∥ B05 ∥ B06) → B07.**
`∥` — независимы, можно одним сообщением с несколькими диспатчами.

Отложенные находки: [FINDINGS.md](FINDINGS.md). Пусто — хорошо.
Замеренные свойства платформы: [PLATFORM.md](PLATFORM.md) — восемь вопросов, версия `2.1.220`,
2026-07-29 и 2026-07-30.

---

## Реструктуризация каталога скиллов

Решение и его обоснование — [R00-skills-restructuring.md](R00-skills-restructuring.md), один дом.
Каталог откатан к 48 дослитийным скиллам (`1d72f0c`), цена откатa — F-57.

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| R01 | вычистка: лексика, descriptions, `when_to_use`, `paths` — 48 файлов | ядро | — | ☑ |
| R02 | слияния 48 → 30, зачистка ссылок, четыре правила F-57 | ядро | R01 | ☑ |
| R03 | `skills:` у ролей по признаку «всегда ли применяется», `Skill` в `tools:` | адаптер | R02 | ☑ |

Порядок строгий: **R01 → R02 → R03.** Обоснование порядка — в R00 §Порядок.
**Все три закрыты.** `/adw:build` снова работоспособен: с `1d72f0c` он был знаемо сломан.

### Итог R01 + R02, замеренный

| | до (48 скиллов, `1d72f0c`) | после (30 скиллов) |
|---|---|---|
| скиллов | 48 | **30** |
| байт каталога | 518 069 | **458 463** (−11%) |
| строк | 7 957 | 8 394 (+5% — перенос по ~100 колонок, не содержание) |
| сумма `description` | 30 508 | **9 342** (−69%) |
| `description` + `when_to_use` | — | 13 111, ни одного за потолком 1536 |
| лексика мёртвых поколений | 250+ | **0** |
| висячих имён скиллов | 31 | **0** |
| `paths` | 0 | 24 из 30 |
| предзагрузка у implementer'а | 9 тел | **3** |

Шесть скиллов без `paths` осознанно: `architecture`, `python-style`, `conventions`,
`meta-skill-author`, `meta-uc-author`, `patterns` — кросс-слойные или кросс-layer, глоб там
не исключил бы ничего. `conventions` — единственный без `When to use vs. neighbours` и
`Hard stops`: это реестр производных, у него нет соседей и нет артефакта, на котором можно
остановиться.

**Одно отступление от R00:** `infra-persistence` — 540 строк против обещанного порога ~500.
Причина в переносе строк, не в содержании: байт там стало меньше (26 986 → 25 867). Роутер
с topic-файлами сознательно не делался — это форма, которую F-10 измеряет как сломанную.

Проверки R02, воспроизводимые:

```bash
# висячие имена скиллов — должно вернуть только python-multipart (это пакет PyPI)
# имена в бэктиках с префиксом слоя, которых нет каталогом в дереве
# дубли вида `X` … `X` в одной строке — только client/client в таблице профилей
find plugins/adw -name '*.py' -o -name '*.sh' -o -name 'hooks.json' -o -name 'anchors.json'
```

---

## Протокол цепочки (исполняет главная сессия)

Промпт, которым запускается сессия-оркестратор: [ORCHESTRATE.md](ORCHESTRATE.md). Он несёт только
осанку оркестратора и его собственные соблазны; сам протокол — здесь, и второй его копии нет.

**Сначала — не давать машине уснуть.** В фоне, один раз на всю цепочку:

```bash
caffeinate -dimsu
```

Затем на каждую задачу, по порядку зависимостей:

1. **Диспатч.** Один агент на одну задачу, тип по таблице ниже. В промпте — только путь к файлу
   задачи и требование прочитать всё, что названо в её секции «Читать сначала». Ничего больше:
   задача самодостаточна, а пересказ её в промпте — это второй источник правды.
2. **Warden.** По возвращении builder'а — диспатч `adw-warden` со списком изменённых файлов
   (`git status --short` + `git diff`). Warden read-only, он не чинит, он выносит
   `ПРИНЯТЬ` / `ОТКЛОНИТЬ` со ссылкой на правило.
3. **Проверка.** Главная сессия сама прогоняет секцию «Проверка» файла задачи. Не веря отчёту
   builder'а на слово — это тот же принцип, по которому в самом воркфлоу вердикт выносит не автор.
4. **Ветвление.**
   - Warden `ПРИНЯТЬ` + проверка зелёная → коммит, галочка в таблице выше, следующая задача.
   - Warden `ОТКЛОНИТЬ` → повторный диспатч builder'а с вердиктом warden'а. Три круга на задачу,
     дальше остановка и вопрос человеку.
   - Проверка красная → повторный диспатч с конкретикой провала.
5. **Находки.** Всё, что builder или warden нашли, но что не входит в Deliverables задачи, пишет
   **главная сессия** в `FINDINGS.md`. Не builder — иначе находка превращается в правку.

### Типы агентов

| Тип | Для чего | Инструменты |
|---|---|---|
| `adw-prober` | замерить поведение платформы экспериментом; записывает только замеренное | Read, Write, Bash, Glob, Grep, WebFetch |
| `adw-builder` | написать прозаические артефакты задачи (агенты, команды, шаблоны) | Read, Write, Edit, Bash, Glob, Grep |
| `adw-warden` | сверить результат с красными линиями и §8; **read-only, не чинит** | Read, Bash, Glob, Grep |

Определения — в `.claude/agents/`. Это **project-scoped** агенты: они не под `plugins/adw/`,
значит не шипятся. Каталог создан заново, поэтому **до первого диспатча нужен рестарт сессии** —
watcher платформы покрывает только каталоги, существовавшие на старте сессии. Без рестарта
fallback: диспатч `general-purpose` с инструкцией «прочитай `.claude/agents/<имя>.md` и следуй ему».

---

## Правила, которые делают этот список ограниченным

Прошлый заход декомпозировался в 11 задач и вырос до 64 — 44 производные задачи родились в
процессе. Три правила против этого, и warden проверяет каждое:

1. **Восьмая задача — видимое событие.** Новая задача не добавляется builder'ом и не появляется
   «по ходу». Находка идёт в `FINDINGS.md`; превращение находки в задачу — решение человека,
   отдельным коммитом, с ответом на вопрос «какой измеренный провал она лечит».
2. **Задача не растёт за свои Deliverables.** Обнаружил рядом проблему — записал в находки, не
   починил. Это не бюрократия: 44 производные задачи выросли именно из «пока я здесь, поправлю и
   вот это».
3. **Этот файл — таблица, а не журнал.** У прошлого захода INDEX разросся до 61 КБ прозы. Здесь
   одна строка на задачу и статус. Рассуждения живут в файле задачи, находки — в `FINDINGS.md`.
