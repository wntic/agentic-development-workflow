# plan/ — реализация шага 1, статус и протокол

Шаг 1 из `WORKFLOW.md` §10: **адаптер**. Четыре агента, три команды, шаблоны, манифесты.
Ноль скриптов — это не пожелание, а красная линия 2 (бюджет enforcement = ноль).

`plan/` — **дев-запись, она не шипится.** Правило поставки по местоположению: файл шипится
тогда и только тогда, когда лежит под `plugins/adw/`.

---

## Статус

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| B01 | [платформенная проба](B01-platform-probe.md) | — | — | ☑ |
| B02 | [четыре агента](B02-agents.md) | адаптер | B01 | ☑ |
| B03 | [шаблоны артефактов](B03-templates.md) | ядро | — | ☑ |
| B04 | [`/adw:spec`](B04-spec-command.md) | адаптер | B03 | ☑ |
| B05 | [`/adw:build`](B05-build-command.md) | адаптер | B01, B02 | ☑ |
| B06 | [`/adw:accept`](B06-accept-command.md) | адаптер | B03 | ☑ |
| B07 | [манифесты и репетиция установки](B07-manifests.md) | адаптер | B02, B04, B05, B06 | ☑ |

Порядок: **B01 → (B02 ∥ B03) → (B04 ∥ B05 ∥ B06) → B07.**
`∥` — независимы, можно одним сообщением с несколькими диспатчами.

Отложенные находки: [FINDINGS.md](FINDINGS.md). Пусто — хорошо.
Замеренные свойства платформы: [PLATFORM.md](PLATFORM.md) — восемь вопросов, версия `2.1.220`,
2026-07-29 и 2026-07-30.

---

## Реструктуризация каталога скиллов

Решение и его обоснование — [R00-skills-restructuring.md](R00-skills-restructuring.md), один дом.
Каталог откатан к 48 дослитийным скиллам (`1d72f0c`), цена откатa — F-57.

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| R01 | вычистка: лексика, descriptions, `when_to_use`, `paths` — 48 файлов | ядро | — | ☑ |
| R02 | слияния 48 → 30, зачистка ссылок, четыре правила F-57 | ядро | R01 | ☑ |
| R03 | `skills:` у ролей по признаку «всегда ли применяется», `Skill` в `tools:` | адаптер | R02 | ☑ |

Порядок строгий: **R01 → R02 → R03.** Обоснование порядка — в R00 §Порядок.
**Все три закрыты.** `/adw:build` снова работоспособен: с `1d72f0c` он был знаемо сломан.

### Итог R01 + R02, замеренный

| | до (48 скиллов, `1d72f0c`) | после (30 скиллов) |
|---|---|---|
| скиллов | 48 | **30** |
| байт каталога | 518 069 | **458 463** (−11%) |
| строк | 7 957 | 8 394 (+5% — перенос по ~100 колонок, не содержание) |
| сумма `description` | 30 508 | **9 342** (−69%) |
| `description` + `when_to_use` | — | 13 111, ни одного за потолком 1536 |
| лексика мёртвых поколений | 250+ | **0** |
| висячих имён скиллов | 31 | **0** |
| `paths` | 0 | 24 из 30 |
| предзагрузка у implementer'а | 9 тел | **3** |

Шесть скиллов без `paths` осознанно: `architecture`, `python-style`, `conventions`,
`meta-skill-author`, `meta-uc-author`, `patterns` — кросс-слойные или кросс-layer, глоб там
не исключил бы ничего. `conventions` — единственный без `When to use vs. neighbours` и
`Hard stops`: это реестр производных, у него нет соседей и нет артефакта, на котором можно
остановиться.

**Одно отступление от R00:** `infra-persistence` — 540 строк против обещанного порога ~500.
Причина в переносе строк, не в содержании: байт там стало меньше (26 986 → 25 867). Роутер
с topic-файлами сознательно не делался — это форма, которую F-10 измеряет как сломанную.

Проверки R02, воспроизводимые:

```bash
# висячие имена скиллов — должно вернуть только python-multipart (это пакет PyPI)
# имена в бэктиках с префиксом слоя, которых нет каталогом в дереве
# дубли вида `X` … `X` в одной строке — только client/client в таблице профилей
find plugins/adw -name '*.py' -o -name '*.sh' -o -name 'hooks.json' -o -name 'anchors.json'
```

---

## Шаг 4 §10 — решения по находкам

Проход законен впервые: красная линия 3 удовлетворена (три фичи против двух проходов правок).
Материал ко входу — [S04-TRIAGE.md](S04-TRIAGE.md), снимок на 2026-07-31 **до** решений. Сами
решения — в [FINDINGS.md](FINDINGS.md), секция «Шаг 4, проход первый»; где она расходится с
триажем, права она.

Проход закрыл 15 записей без единой правки (8 закрылись побочно, 7 — решением «не наступило») и
принял решения по оставшимся 16. **Новый механизм ровно один — скаффолдинг.**

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| S04-01 | [замер `tools:` — 4 прогона](S04-01-tools-resolution.md) | — | — | ☑ |
| S04-02 | [канон догоняет решения](S04-02-canon.md) | ядро | S04-01 | ☑ |
| S04-03 | [скаффолдинг в адаптере](S04-03-scaffold-adapter.md) | адаптер | S04-02 | ☑ |
| S04-04 | [ветка и baseline SHA](S04-04-cycle-artifacts.md) | адаптер + ядро | S04-02 | ☑ |
| S04-05 | [смысловой маркер](S04-05-marker-slug.md) | ядро + адаптер | S04-02 | ☑ |
| S04-06 | [форма шаблонов + платформа](S04-06-skill-format-and-platform.md) | ядро | S04-01 | ☑ |
| S04-07 | [шаг возврата по CONTRACT-CHANGE](S04-07-contract-change-route.md) | ядро | S04-03 | ☑ |
| S04-08 | [маркер в двух ролях](S04-08-marker-in-roles.md) | ядро | S04-05 | ☑ |
| S04-09 | [срез врезки ORIENT §4, команда чтения реестра](S04-09-maps-cut.md) | дев-запись | — | ☑ |
| S04-10 | [четыре находки с реальным риском](S04-10-four-real-risks.md) | ядро + адаптер | — | ☑ |
| S04-11 | [канон догоняет собственные правки](S04-11-canon-catchup.md) | ядро + адаптер | — | ☑ |

Порядок: **S04-01 → S04-02 → (S04-03 ∥ S04-04 ∥ S04-05) ∥ S04-06.**
S04-01 идёт первым не только по зависимости: его прогон 2 может уточнить формулировку решения по
F-04, а её несёт правка 6 задачи S04-02.

**Change 004 поставлен 2026-08-02.** Дефект-лог — `FINDINGS.md`, секция «Шаг 4+». Счёт по красной
линии 3 стал **4 фичи : 3 прохода**, то есть запас на следующую итерацию воркфлоу есть.

Это был замер скаффолдинга, и он удался: 35 падений против скелета, **каждое — ассерт о поведении
либо обращение к пустому телу**, ни одного collection error; восемь из девяти тестовых файлов 004
несут ноль отложенных импортов. Скелет попутно нашёл дефект миграции, который иначе ронял бы каждую
свежую базу. Новые находки прогона: **F-93…F-96**.

---

## Исполнение решений интервью — S05

Решения — `FINDINGS.md`, секция «Решения человека, 2026-08-02 — интервью по открытым находкам».
Двадцать две записи решены, семь закрыты без правки, новых механизмов ноль. Здесь исполняются
четырнадцать, требующих текста, плюс одно действие вне репозитория.

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| S05-01 | [оговорки в таблице проверок](S05-01-maps-clauses.md) | дев-запись | — | ☑ |
| S05-02 | [легенда и даты `PLATFORM.md`](S05-02-platform-legend.md) | дев-запись | — | ☑ |
| S05-03 | [канон: команды, потолок, число](S05-03-canon-counts.md) | ядро + дев-запись | S05-01 | ☑ |
| S05-04 | [имя секции шаблона и фронтматтер](S05-04-template-heading.md) | ядро + дев-запись | S05-01 | ☑ |
| S05-05 | [пути цикла: baseline, резюм, ярлык](S05-05-cycle-paths.md) | адаптер + ядро | S05-03 | ☑ |
| S05-06 | [builder и ненайденная цитата](S05-06-builder-adjacent-ruling.md) | дев-запись | — | ☑ |

Порядок диктуется не темой, а файлами: два агента не работают в одном файле.
**(S05-01 ∥ S05-02 ∥ S05-06) → (S05-03 ∥ S05-04) → S05-05.**
`S05-01` держит `ORIENT.md` и `CLAUDE.md`, поэтому `S05-03` и `S05-04` ждут её; `S05-05` пишет
командную сторону правила о потолке и обязана прочесть каноническую, которую пишет `S05-03`.

**Вне этих задач и вне репозитория: исполнено 2026-08-02**, коммит `eecc6d5` в базовой ветке
`adw-probe`. 22 ординала → слаги (30 различимых слагов на 30 критериев с тестами), плюс снос костыля
отложенного импорта: три блока `TYPE_CHECKING` и 29 импортов внутри тел, −164 строки против +57.
`make check` зелёный — 113 тестов, интеграционный ярус против настоящего PostgreSQL. Подробности и
находка прохода (F-109) — в `FINDINGS.md`, секция «F-93 исполнена».

---

## Разбор прогона 005 — S06

Решения — `FINDINGS.md`, секция «Находки разбора прогона 005», интервью 2026-08-02. Три записи, все
решены; новых механизмов ноль, F-110 сужает существующий запрет вместо того, чтобы заводить гард.

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| S06-01 | [фейк в коммите скелета](S06-01-fake-in-skeleton.md) | ядро + адаптер | — | ☑ |
| S06-02 | [что читать, разбирая прогон](S06-02-orient-what-to-read.md) | дев-запись | — | ☑ |
| S06-03 | [красные по агентам](S06-03-report-red-aggregate.md) | вне репозитория | — | ☑ |

Все три независимы — множества файлов не пересекаются, и S06-03 правит другой репозиторий. Проверки
написаны через `git diff --stat -- <свои пути>`, а не через `git status`: F-99 измерила, что при
параллельных диспатчах вторая форма даёт ложный красный на честной работе.

---

## Претензии человека к коду пробы — S07

Три претензии, разобранные 2026-08-02 чтением кода и замерами. Итог разбора: **скиллам нужна одна
правка, а не три** — `conventions` уже предписывает `per-file-ignores` для `F403/F405` и прямо
запрещает инлайновый `noqa`, а `architecture` верно объясняет, зачем нужен блок `from . import`
(замерено: без него `mypy --strict` даёт `name-defined`, рантайм при этом работает — как в `asyncio`,
который просто не проверяется mypy). Расходится с house style **проба**, а не каталог.

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| S07-01 | [дом правила про комментарии](S07-01-comments-rule-home.md) | ядро | — | ☑ |
| S07-02 | [housekeeping в пробе](S07-02-probe-housekeeping.md) | вне репозитория | S07-01 | ☑ |

**Исполнено 2026-08-02**, коммит `46369e5` в `main` пробы: комментарии 103 → 26 (12.7% → 3.6%),
`noqa` 33 → 0, тулчейн приведён к скиллу. Эквивалентность поведения доказана сравнением потоков
токенов — 29 файлов из 29 идентичны. `make check` зелёный, 140 тестов. Находки: **F-119…F-121**.

Третья претензия — возврат `CreateShortLinkHandler` — в housekeeping **не входит**: `application`
предписывает `UUID` для create, здесь идентичность это сам код (естественный ключ), а предложение
человека подразумевает суррогатный первичный ключ. Это смена контракта и идёт спекой.

---

## Прогон 006 — счётчик переходов

**Поставлено 2026-08-02**, тег `change/006` в `adw-probe`, дельта удалена приёмкой. Первое изменение
глубины **L** и первое, где сработал структурный триггер (5 — меняется опубликованный контракт), то
есть **первая написанная секция `Design`**: у 001–005 не сработал ни один триггер. Одиннадцать
критериев, десять `[x]`, AC-10 `[m]`. Исходника 57 строк, всего 1273.

Прогон чистый: шесть ролей, 394 хода, 243 вызова инструмента, ноль отказов; 42 прогона тулчейна, 20
красных, **ни один не случаен**. Правок в `tests/**` после baseline — ноль. Адверсарный проход
оценщика: четыре ложных имплементации, три поймано, одна нет — и та названа заранее в `Verification`.

Разбор — `FINDINGS.md`, секция «Находки разбора прогона 006»: **F-125…F-127**. Замеры самого прогона
(слепота к лоссовому `record_follow`, тир AC-11) лежат не здесь, а в живой спеке пробы, встроенные в
ослабляемые инварианты — так велит F-112.

**Красная линия 3.** 006 гасит один из трёх проходов, названных F-124: счёт по журналу — **6 фич
против 6 проходов**, выведено из строки «4 фичи : 3 прохода» после 004 плюс 005, S05, S06, S07 и 006.
F-124 закрыта решением человека 2026-08-03: фича была следующим действием и она поставлена.

**Чор в пробе, которого не было в записи.** `5d61cd5` (2026-08-02, после поставки 006): ограда
`_RejectReadsShortLinkRepo` читает имена с порта `IShortLinkRepository` и отвергает всё, кроме `add`,
плюс две строки живой спеки, правленные **вне `/adw:accept`**. Прецедент второго рода поставлен
сознательно — приёмка больше не единственный писатель файла способности. **В счёт проходов не идёт:**
работа в целевом проекте, а не итерация воркфлоу (решение человека 2026-08-03).

---

## Исполнение решений интервью по всем открытым находкам — S08

Решения — `FINDINGS.md`, секция «Решения человека, 2026-08-03 — интервью по всем открытым находкам».
**Двадцать пять записей решены, новых механизмов ноль.** Три закрылись не выбором развилки, а тем, что
развилка оказалась ложной (F-120 снялась решением по F-121, F-115 сузилась замером прогона 006, F-99
обнаружилась исполненной дважды). Здесь исполняются требующие текста плюс два действия вне репозитория.

| # | Задача | Слой | Зависит от | Статус |
|---|---|---|---|---|
| S08-01 | [таблица проверок и `rtk proxy`](S08-01-orient-checks.md) | дев-запись | — | ☐ |
| S08-02 | [легенды и число в хребте](S08-02-legends-and-canon.md) | дев-запись + ядро | — | ☐ |
| S08-03 | [охват комментариев, `line-length`](S08-03-comments-scope-and-line-length.md) | ядро | — | ☐ |
| S08-04 | [формы у двух правил `application`](S08-04-application-forms.md) | ядро | — | ☐ |
| S08-05 | [маршрут тупика, фейк, `description`](S08-05-roles-route-and-report.md) | адаптер | — | ☐ |
| S08-06 | [housekeeping в пробе](S08-06-probe-housekeeping.md) | вне репозитория | S08-03 | ☐ |
| S08-07 | [счёт прогонов в таймлайне](S08-07-report-run-counting.md) | вне репозитория | — | ☐ |

Порядок диктуется файлами, не темами: **(S08-01 ∥ S08-02 ∥ S08-03 ∥ S08-04 ∥ S08-05 ∥ S08-07) →
S08-06.** Множества файлов не пересекаются ни в одной паре; S08-06 ждёт S08-03, потому что приводит
пробу к правилу, которое S08-03 пишет — та же зависимость, что у S07-02 от S07-01.

**Протокол правит только главная сессия:** две новые строки в нём (шаг «решения в реестр до файлов
задач» по F-113 и форма проверки по F-99) написаны здесь, а не задачей, чтобы в `INDEX.md` не сошлись
два писателя.

Записи, закрытые **без правки** и потому в таблице не представленные: F-108 (правки нет, сигнал к
переоткрытию — первый отчёт, где резюм состоялся и не виден), F-118 (единственная непугадывающая
починка дороже пользы), F-124 (закрыта поставкой 006), F-106 (чтение подтверждено человеком).

---

## Шаг 5 §10 — отложен

**Решение человека, 2026-08-02.** Харвест скиллов из реального кода откладывается без срока: каталог
только что приведён к текущей форме вручную (`R01`+`R02`, 30 скиллов), а источник правок по §11.6 —
измеренный разрыв между тем, что написал агент, и тем, что человек написал бы сам — требует
приложения, которое человек ведёт сам; `adw-probe` таким приложением не является.

Записано в `WORKFLOW.md` §10 и §11.6, обоснование — `FINDINGS.md`, секция «Решение человека,
2026-08-02». Открывается, когда появится такое приложение и будет записан первый разрыв. Заложников у
отложенного шага нет: обе указывавшие на него записи (F-06, F-45) закрыты реструктуризацией R01,
проверено грепом по дереву.

---

## Протокол цепочки (исполняет главная сессия)

Промпт, которым запускается сессия-оркестратор: [ORCHESTRATE.md](ORCHESTRATE.md). Он несёт только
осанку оркестратора и его собственные соблазны; сам протокол — здесь, и второй его копии нет.

**Сначала — не давать машине уснуть.** В фоне, один раз на всю цепочку:

```bash
caffeinate -dimsu
```

**И до первого файла задачи — решения в реестр.** Диспозиции человека ложатся в `FINDINGS.md`
**раньше**, чем написан первый файл задачи, который на них ссылается. Не «до диспатча» — до
**написания**: задача, ссылающаяся на решение, которого в реестре нет, оставляет исполнителю и
warden'у нечего открыть, кроме самой задачи (F-113, три исполнителя поймали это независимо).

Затем на каждую задачу, по порядку зависимостей:

1. **Диспатч.** Один агент на одну задачу, тип по таблице ниже. В промпте — только путь к файлу
   задачи и требование прочитать всё, что названо в её секции «Читать сначала». Ничего больше:
   задача самодостаточна, а пересказ её в промпте — это второй источник правды.
   **Секция «Проверка» задачи предъявляет свой дифф по своим путям** — `git diff --stat -- <пути>`, —
   а не состояние всего дерева: при параллельных диспатчах без коммита между ними `git status`
   даёт ложный красный на честной работе (F-99, поймано двумя builder'ами независимо).
2. **Warden.** По возвращении builder'а — диспатч `adw-warden` со списком изменённых файлов
   (`git status --short` + `git diff`). Warden read-only, он не чинит, он выносит
   `ПРИНЯТЬ` / `ОТКЛОНИТЬ` со ссылкой на правило.
3. **Проверка.** Главная сессия сама прогоняет секцию «Проверка» файла задачи. Не веря отчёту
   builder'а на слово — это тот же принцип, по которому в самом воркфлоу вердикт выносит не автор.
4. **Ветвление.**
   - Warden `ПРИНЯТЬ` + проверка зелёная → коммит, галочка в таблице выше, следующая задача.
   - Warden `ОТКЛОНИТЬ` → повторный диспатч builder'а с вердиктом warden'а. Три круга на задачу,
     дальше остановка и вопрос человеку.
   - Проверка красная → повторный диспатч с конкретикой провала.
5. **Находки.** Всё, что builder или warden нашли, но что не входит в Deliverables задачи, пишет
   **главная сессия** в `FINDINGS.md`. Не builder — иначе находка превращается в правку.

### Типы агентов

| Тип | Для чего | Инструменты |
|---|---|---|
| `adw-prober` | замерить поведение платформы экспериментом; записывает только замеренное | Read, Write, Bash, Glob, Grep, WebFetch |
| `adw-builder` | написать прозаические артефакты задачи (агенты, команды, шаблоны) | Read, Write, Edit, Bash, Glob, Grep |
| `adw-warden` | сверить результат с красными линиями и §8; **read-only, не чинит** | Read, Bash, Glob, Grep |

Определения — в `.claude/agents/`. Это **project-scoped** агенты: они не под `plugins/adw/`,
значит не шипятся. Каталог создан заново, поэтому **до первого диспатча нужен рестарт сессии** —
watcher платформы покрывает только каталоги, существовавшие на старте сессии. Без рестарта
fallback: диспатч `general-purpose` с инструкцией «прочитай `.claude/agents/<имя>.md` и следуй ему».

---

## Правила, которые делают этот список ограниченным

Прошлый заход декомпозировался в 11 задач и вырос до 64 — 44 производные задачи родились в
процессе. Три правила против этого, и warden проверяет каждое:

1. **Восьмая задача — видимое событие.** Новая задача не добавляется builder'ом и не появляется
   «по ходу». Находка идёт в `FINDINGS.md`; превращение находки в задачу — решение человека,
   отдельным коммитом, с ответом на вопрос «какой измеренный провал она лечит».
2. **Задача не растёт за свои Deliverables.** Обнаружил рядом проблему — записал в находки, не
   починил. Это не бюрократия: 44 производные задачи выросли именно из «пока я здесь, поправлю и
   вот это».
3. **Этот файл — таблица, а не журнал.** У прошлого захода INDEX разросся до 61 КБ прозы. Здесь
   одна строка на задачу и статус. Рассуждения живут в файле задачи, находки — в `FINDINGS.md`.
