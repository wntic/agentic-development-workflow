# ─────────────────────────────────────────────────────────────────────────────
# GENERATED — DO NOT EDIT.  Regenerate:  uv run .claude/tools/gen_template.py
# ─────────────────────────────────────────────────────────────────────────────
# This is the manifest SHAPE skeleton, emitted from the validator's `SCHEMAS`
# (validate_manifest.py — the single executable contract for the manifest form).
# It exists so the shape can be READ without drifting from what the validator enforces.
#
#   • It is NOT a valid manifest — values are type placeholders and cross-references
#     (handler deps → protocols, endpoint.handler → command, …) do not resolve.
#   • For a validated WORKED EXAMPLE, read a fixture:
#       .claude/tools/fixtures/helpdesk_manifest.yaml   (auth + tickets)
#       .claude/tools/fixtures/vector_rag_manifest.yaml (polyglot + non-CRUD)
#       .claude/tools/fixtures/label_manifest.yaml      (small CRUD)
#   • For field SEMANTICS (why a field exists, earn-its-place, derived-not-declared),
#     read codegen_workflow_spec.md §5 and the `conventions` skill — semantics live in
#     their canonical layer and are never restated here.
#
# Each line is annotated:  <field>: <placeholder>   # <type>, required|optional[, default ...]

meta:                                               # Meta (mapping), required
  epic: "<str>"                                     # str, required
  name: "<str>"                                     # str, required
  sources: ["<str>"]                                # list of str, required
  supersedes: ["<str>"]                             # list of str, optional
  replaces: ["<str>"]                               # list of str, optional
  notes: "<str>"                                    # str, optional

domain:                                             # Domain (mapping), optional
  enums:                                            # list of Enum, optional
    - name: "<str>"                                 # str, required
      subdomain: "<str>"                            # str, required
      base: StrEnum                                 # one of: StrEnum, Enum, optional, default 'StrEnum'
      members:                                      # list of EnumMember, required
        - name: "<str>"                             # str, required
          value: "<str>"                            # str, required
      methods:                                      # list of EnumMethod, optional
        - signature: "<str>"                        # str, required
          rule: "<str>"                             # str, required
      sources: ["<str>"]                            # list of str, required
  value_objects:                                    # list of ValueObject, optional
    - name: "<str>"                                 # str, required
      subdomain: "<str>"                            # str, required
      frozen: false                                 # bool, optional, default True
      fields:                                       # list of Field, required
        - name: "<str>"                             # str, required
          type: "<str>"                             # str, required
          optional: false                           # bool, optional
          default: <any>                            # any literal (validator never inspects), optional
      invariants:                                   # list of Invariant, optional
        - rule: "<str>"                             # str, required
          field: "<str>"                            # str, optional
          source: "<str>"                           # str, required
      sources: ["<str>"]                            # list of str, required
  entities:                                         # list of Entity, optional
    - name: "<str>"                                 # str, required
      subdomain: "<str>"                            # str, required
      identity_field: "<str>"                       # str, optional, default 'id'
      fields:                                       # list of Field, required
        - name: "<str>"                             # str, required
          type: "<str>"                             # str, required
          optional: false                           # bool, optional
          default: <any>                            # any literal (validator never inspects), optional
      invariants:                                   # list of Invariant, optional
        - rule: "<str>"                             # str, required
          field: "<str>"                            # str, optional
          source: "<str>"                           # str, required
      sources: ["<str>"]                            # list of str, required
  services:                                         # list of Service, optional
    - name: "<str>"                                 # str, required
      subdomain: "<str>"                            # str, required
      dependencies: ["<str>"]                       # list of str, optional
      methods:                                      # list of ServiceMethod, required
        - signature: "<str>"                        # str, required
          raises: ["<str>"]                         # list of str, optional
          behaviour:                                # list of Behaviour, optional
            - given: "<str>"                        # str, required
              arrange:                              # list of Seed, optional
                - entity: "<str>"                   # str, required
                  fields: {}                        # free key→value map, optional
              when: "<str>"                         # str, required
              act: {}                               # free key→value map, optional
              then:                                 # Then (mapping), required
                raises: "<str>"                     # str, optional
                returns: "<str>"                    # str, optional
                persists: "<str>"                   # str, optional
                deletes: "<str>"                    # str, optional
                logs: "<str>"                       # str, optional
                calls: "<str>"                      # str, optional
                with: {}                            # free key→value map, optional
              source: "<str>"                       # str, required
          notes: "<str>"                            # str, optional
      notes: "<str>"                                # str, optional
      sources: ["<str>"]                            # list of str, required
  filters:                                          # list of Filter, optional
    - name: "<str>"                                 # str, required
      subdomain: "<str>"                            # str, required
      fields:                                       # list of Field, required
        - name: "<str>"                             # str, required
          type: "<str>"                             # str, required
          optional: false                           # bool, optional
          default: <any>                            # any literal (validator never inspects), optional
      pagination: false                             # bool, optional
      sort:                                         # FilterSort (mapping), optional
        enum_name: "<str>"                          # str, required
        keys: ["<str>"]                             # list of str, required
        default: "<str>"                            # str, required
      sources: ["<str>"]                            # list of str, required
  repository_protocols:                             # list of RepositoryProtocol, optional
    - name: "<str>"                                 # str, required
      subdomain: "<str>"                            # str, required
      aggregate: "<str>"                            # str, required
      methods:                                      # list of "signature" | {signature, notes}, required
        - "async def method(self, arg: Type) -> ReturnType"
      raises: ["<str>"]                             # list of str, optional
      sources: ["<str>"]                            # list of str, required
  capability_protocols:                             # list of CapabilityProtocol, optional
    - name: "<str>"                                 # str, required
      subdomain: "<str>"                            # str, required
      methods:                                      # list of "signature" | {signature, notes}, required
        - "async def method(self, arg: Type) -> ReturnType"
      sources: ["<str>"]                            # list of str, required
      notes: "<str>"                                # str, optional
  exceptions:                                       # list of DomainException, optional
    - name: "<str>"                                 # str, required
      code: "<str>"                                 # str, required
      http_status: 0                                # int, required
      sources: ["<str>"]                            # list of str, required

application:                                        # Application (mapping), optional
  commands:                                         # list of Command, optional
    - name: "<str>"                                 # str, required
      input:                                        # list of Field, optional
        - name: "<str>"                             # str, required
          type: "<str>"                             # str, required
          optional: false                           # bool, optional
          default: <any>                            # any literal (validator never inspects), optional
      output: "<str>"                               # str, required
      handler:                                      # Handler (mapping), required
        dependencies: ["<str>"]                     # list of str, optional
      log_event: "<str>"                            # str, optional
      raises: ["<str>"]                             # list of str, optional
      behaviour:                                    # list of Behaviour, optional
        - given: "<str>"                            # str, required
          arrange:                                  # list of Seed, optional
            - entity: "<str>"                       # str, required
              fields: {}                            # free key→value map, optional
          when: "<str>"                             # str, required
          act: {}                                   # free key→value map, optional
          then:                                     # Then (mapping), required
            raises: "<str>"                         # str, optional
            returns: "<str>"                        # str, optional
            persists: "<str>"                       # str, optional
            deletes: "<str>"                        # str, optional
            logs: "<str>"                           # str, optional
            calls: "<str>"                          # str, optional
            with: {}                                # free key→value map, optional
          source: "<str>"                           # str, required
      sources: ["<str>"]                            # list of str, required
      notes: "<str>"                                # str, optional
  queries:                                          # list of Query, optional
    - name: "<str>"                                 # str, required
      input:                                        # list of Field, optional
        - name: "<str>"                             # str, required
          type: "<str>"                             # str, required
          optional: false                           # bool, optional
          default: <any>                            # any literal (validator never inspects), optional
      output: "<str>"                               # str, required
      result_fields:                                # list of Field, optional
        - name: "<str>"                             # str, required
          type: "<str>"                             # str, required
          optional: false                           # bool, optional
          default: <any>                            # any literal (validator never inspects), optional
      result_dtos:                                  # list of ResultDto, optional
        - name: "<str>"                             # str, required
          fields:                                   # list of Field, required
            - name: "<str>"                         # str, required
              type: "<str>"                         # str, required
              optional: false                       # bool, optional
              default: <any>                        # any literal (validator never inspects), optional
      handler:                                      # Handler (mapping), required
        dependencies: ["<str>"]                     # list of str, optional
      raises: ["<str>"]                             # list of str, optional
      behaviour:                                    # list of Behaviour, optional
        - given: "<str>"                            # str, required
          arrange:                                  # list of Seed, optional
            - entity: "<str>"                       # str, required
              fields: {}                            # free key→value map, optional
          when: "<str>"                             # str, required
          act: {}                                   # free key→value map, optional
          then:                                     # Then (mapping), required
            raises: "<str>"                         # str, optional
            returns: "<str>"                        # str, optional
            persists: "<str>"                       # str, optional
            deletes: "<str>"                        # str, optional
            logs: "<str>"                           # str, optional
            calls: "<str>"                          # str, optional
            with: {}                                # free key→value map, optional
          source: "<str>"                           # str, required
      sources: ["<str>"]                            # list of str, required
      notes: "<str>"                                # str, optional

infrastructure:                                     # Infrastructure (mapping), optional
  settings:                                         # list of Settings, optional
    - name: "<str>"                                 # str, required
      env_prefix: "<str>"                           # str, required
      fields:                                       # list of SettingsField, required
        - name: "<str>"                             # str, required
          type: "<str>"                             # str, required
          default: <any>                            # any literal (validator never inspects), optional
          secret: false                             # bool, optional
      methods:                                      # list of SettingsMethod, optional
        - signature: "<str>"                        # str, required
          decorators: ["<str>"]                     # list of str, optional
          notes: "<str>"                            # str, optional
      sources: ["<str>"]                            # list of str, required
  datastores:                                       # list of Datastore, optional
    - name: "<str>"                                 # str, required
      kind: "<str>"                                 # str, required
      settings: "<str>"                             # str, optional
      requires_packages: ["<str>"]                  # list of str, optional
      sources: ["<str>"]                            # list of str, required
  repositories:                                     # list of Repository, optional
    - implements: "<str>"                           # str, required
      backs: "<str>"                                # str, required
      store: "<str>"                                # str, optional
      notes: "<str>"                                # str, optional
      sources: ["<str>"]                            # list of str, required
  capabilities:                                     # list of Capability, optional
    - implements: "<str>"                           # str, required
      adapter: "<str>"                              # str, required
      role: "<str>"                                 # str, optional
      settings: "<str>"                             # str, optional
      requires_packages: ["<str>"]                  # list of str, optional
      notes: "<str>"                                # str, optional
      sources: ["<str>"]                            # list of str, required

restapi:                                            # RestApi (mapping), optional
  schemas:                                          # list of RestSchema, optional
    - name: "<str>"                                 # str, required
      resource: "<str>"                             # str, required
      kind: request                                 # one of: request, response, required
      fields:                                       # list of Field, required
        - name: "<str>"                             # str, required
          type: "<str>"                             # str, required
          optional: false                           # bool, optional
          default: <any>                            # any literal (validator never inspects), optional
      sources: ["<str>"]                            # list of str, required
  endpoints:                                        # list of Endpoint, optional
    - method: GET                                   # one of: GET, POST, PATCH, PUT, DELETE, required
      path: "<str>"                                 # str, required
      resource: "<str>"                             # str, required
      auth: "<str>"                                 # str, required
      request: "<str>"                              # str, optional
      response: "<str>"                             # str, optional
      request_kind: json                            # one of: json, multipart, optional, default 'json'
      status_code: 0                                # int, optional
      handler: "<str>"                              # str, required
      notes: "<str>"                                # str, optional
      sources: ["<str>"]                            # list of str, required
  middlewares:                                      # list of Middleware, optional
    - name: "<str>"                                 # str, required
      config: {}                                    # free key→value map, optional
      introduces_http: [0]                          # list of int, optional
      notes: "<str>"                                # str, optional
      sources: ["<str>"]                            # list of str, required

tests:                                              # Tests (mapping), optional
  architecture_rules:                               # list of ArchitectureRule, optional
    - name: "<str>"                                 # str, required
      pattern: "<str>"                              # str, required
      paths: ["<str>"]                              # list of str, required
      allow_list: ["<str>"]                         # list of str, optional
      sources: ["<str>"]                            # list of str, optional
