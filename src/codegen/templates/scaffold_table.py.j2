from sqlalchemy import Table

from ..metadata import metadata

__all__ = ["{{ table_var }}"]


{% for line in contract %}
{{ line }}
{% endfor %}
{{ table_var }}: Table = Table(
    "{{ table_name }}",
    metadata,
    # TODO: declare the Column(...)s here — import Column and the SQLAlchemy Core types you
    # need (UUID, Text, Integer, Boolean, DateTime, JSONB, …). The primary key is the
    # entity's identity field; nullable mirrors `optional`.
)
